# Mirage Engram DEV-59 — REAL All-inkl read-only host review via connected Opera

Date 2026-09-21. Branch `work/kicom-0.9.27-pam`. Production KiCom 0.9.32 confirmed in original admin. User connected Opera explicitly for read-only server review. Operator's All-inkl KAS and WebFTP tabs and KiCom admin were opened and inspected; **no POST, save, chmod, file changes, credentials or token extraction, installation or activation were performed**.

## Directly observed live facts (not synthetic)

- KAS subdomain configuration maps KiCom to a dedicated subdirectory of the shared hosting account; a secondary provider-generated transitional hostname is also listed by KAS. PHP version configured to 8.5. This establishes a web document-root mapping, **not** unique PHP runtime identity or alias isolation. The secondary hostname must not be treated as a verified HTTPS OAuth origin or granted access.
- WebFTP's shared-account directory listing shows the KiCom web directory and several other application directories as siblings of the private Engram directory. The directory listing gives **the same displayed Unix owner/group for the KiCom directory, private Engram directory and sampled sibling application directories**. The private Engram root has mode 0700; the KiCom and sampled sibling application web roots have mode 0755. A 0700 directory restricts access by *different Unix users*, but does **not** isolate two PHP apps sharing the owner UID. The same-account directory ownership is a directly observed host-architecture risk; **actual PHP process UID, cross-vhost PHP execution/access and configured per-vhost restrictions were NOT established**. Do not infer successful exploit or complete isolation.
- Authenticated original KiCom read-only host audit at `admin.php?engram_host_audit=1` reports `HOST_EVIDENCE_INCOMPLETE_API_INACTIVE`: `webroot=recognized`, `private_root/private_subdirs/host_config/owner_registry/sqlite_file=restricted`, `php_open_basedir=not_set`, `sibling_application_access=readable_sibling_directory_detected`, `vhost_alias_inventory=not_verified`, `php_uid_separation=not_verified`, `cross_app_isolation=not_verified`, `backup_restore=not_verified`, `personal_memory=not_assessed`. These are the application’s own evidence labels; the live read-only report does NOT prove personal-memory disabled/enabled by inspecting full runtime.
- Authenticated setup page `admin.php?engram_setup=1` shows `PREFLIGHT_ONLY`, `data/backups/owners/consent/review/stepup=ready`, `config=existing`, `owners_file=existing`, PHP SQLite ready and the PRE-EXISTING synthetic-memory data DB present, and Passkey owner binding present. **It does not display or prove existence/initialization of the separate DEV-51 OAuth token database and DEV-40 activation DB**; the DEV-58 code remains development-only, not installed.
- Browser navigation to public `/.well-known/oauth-protected-resource` returned **HTTP 404**, consistent with the configured inactive default. A 404 alone does not prove that the rewrite is installed, that metadata is invalid, or that OAuth is ready once activated.

## Decision boundary and concrete next steps

1. **Do NOT attest `host_isolation_verified=true` for personal memories based only on 0700 / same-account WebFTP ownership.** Either prove genuine per-vhost PHP UID/namespace/open_basedir isolation via an authorized server/process diagnostic and KAS operator confirmation, or move the private Engram service to a separately isolated Unix account/service with a narrow authenticated API. Merely applying chmod 0700 inside the present same-owner structure does not solve a same-UID trust boundary.
2. If Christoph authorizes a **synthetic-only** staging pilot before isolation, record the scope: no personal memories, no real user-data import, test-only owner/connector with explicit operator consent, and deliberately limited retention. Do not silently mark production personal-memory flags true or reinterpret a synthetically active host as a privacy attestation.
3. Integrate the CI-tested DEV-58 create-only inactive schema preparation into native first-party admin ONLY after checking the real private config/schema compatibility; the pre-existing user memory/owner/config must remain unchanged. Work plugin installation/connection requires the user's explicit action in supported ChatGPT Work. A genuinely independent new-chat recall remains untested.
4. No user re-testing of prior SQLite or Passkey binding is needed. Any new production host configuration, migration, credential installation or activation requires specific operator approval. Preserve this read-only evidence for the next developer; avoid publishing account identifiers, provider session URLs, private file content and credentials to GitHub/Slack.

**Outcome:** real browser-host assessment completed; concrete shared-ownership isolation gap identified; no production activation claimed and no server mutation performed.
