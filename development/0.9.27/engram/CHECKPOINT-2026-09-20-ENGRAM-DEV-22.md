# KiCom Engram — DEV-22: session-bound memory request adapter; executable synthetic HTTP-like roundtrip

Date: 2026-09-20. User goal is a genuine persistent memory that can be written/read across ChatGPT conversation boundaries; the iOS activator and Slack communication have already been demonstrated. NO additional iOS setup or Mac terminal instructions. This checkpoint contains PUBLIC TECHNICAL METADATA ONLY; no real memories, private paths, identifiers, secrets, tokens or backup contents.

## Implemented now

- NEW `development/0.9.27/engram/KiComEngramDevMemoryAdapter.php`: bounded POST+JSON, DEV bearer headers, existing `KiComDevSessionManager::authenticate` before trusted identity/store/consent callbacks, validated `ENGRAM_REMEMBER` and `ENGRAM_RECALL` operations, private no-store response, reuses DEV-21 identity-scoped `KiComEngramAuthenticatedBridge` and synthetic private SQLite store.
- Critically, an ordinary DEV bearer token authenticates **only the DEV session**; it is NOT proof of a private memory owner or Engram authorization. The adapter requires an INDEPENDENT, server-owned `identityForSession` callback returning an explicitly verified subject, permitted namespaces and `engram.read`/`engram.write` rights. It also requires `consumeApproval` issuing and consuming a fresh exact-record authenticated consent and `openPrivateStore` using verified non-webroot private storage. These trusted functions MUST be supplied by a future reviewed KiCom integration, never by client payload, Slack message or public source.
- NEW `test-engram-dev-memory-adapter.php`, included in full Engram regression `test-engram.php`. A synthetic write is persisted through the request adapter; an independently issued new DEV session bound to the same verified synthetic owner recovers it by query after reopening the SQLite file. Different owner, unbound session, invalid or revoked token, forged request subject or consent, replayed consent, GET, content-type and oversized requests are rejected.
- Non-installable fixed-list review archive includes the new adapter and its test. No change to the trusted original R3 sources/kernel, no live endpoint registered, no credentials or private runtime config in the package. The REVIEW-ONLY KiCom 0.9.27 host-probe candidate still intentionally does NOT install this memory adapter.

## Executed CI and failure correction

Initial combined test run 35522299902 failed because the new standalone test tried to redeclare the DEV session class already loaded by another suite; fixed by reusing the class when present. Run 35522344900 then failed solely on test expectation for a forbidden operation accompanied by an empty JSON object that PHP decodes into an empty list; fixed fixture to use a well-formed associative payload and test the actual operation gate. **Do not cite either failed run as passing.**

Verified tested SHA **`42e011dfa58005dff73c259733bd6f854f449762`**. GitHub Actions `KiCom private Engram DEV` run **35522422099**, job **106108683321**: completed SUCCESS. Logs: `KICOM_ENGRAM_AUTHENTICATED_BRIDGE_TESTS_PASSED=17`, `KICOM_ENGRAM_DEV_MEMORY_ADAPTER_TESTS_PASSED=21`, specific pass for independently issued new session retrieval and denied cross-subject access; fixed-list review archive includes `source/KiComEngramDevMemoryAdapter.php` and `tests/test-engram-dev-memory-adapter.php`; original KiCom ZIP/native verification `ENGRAM_STAGING_REVIEW_VERIFIER_OK=1`, `ENGRAM_STAGING_REVIEW_INSTALL_ATTEMPTED=0`.
CI link: https://github.com/cschymura/kicom-update-mirror/actions/runs/35522422099

## Exact remaining live integration boundary

The adapter is runnable and its functional protocol tests pass with SYNTHETIC server-trusted identities and approval callbacks. It is **NOT registered in live KiCom**, no HTTP route is enabled, no private real-user memory has been written and no distinct ChatGPT session has retrieved a genuine Engram record from the user's server. Production KiCom still reports 0.9.26 unless reverified. A public GitHub mirror is NOT a private memory store or production trust root.

Next required build: review actual KiCom session/passkey identity-to-subject mapping (do not assume DEV session ID/label is authoritative), implement the trusted server-owned identity+rights resolver and consent receipt issuer, and test private same-UID host isolation and rollback through the existing KiCom protected update path. Only then assemble a complete protected, rollbackable staging package; seek Christoph's explicit approval before actual live installation. Never ask Christoph to recreate the existing iOS trigger or repeat the synthetic Mac/iPhone backup setup. Another worker with no Slack can take up this CONCRETE route integration by reading this exact checkpoint + adapter on GitHub, independently implementing isolated tests and committing public source through its available GitHub connector, rather than looping failed BOOTSTRAP resources.
