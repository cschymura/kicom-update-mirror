# Mirage Engram DEV-57 — native KiCom 0.9.32 rebuilt with exact DEV-56 stable issuer/callback source

**Date:** 2026-09-21. Branch `work/kicom-0.9.27-pam`. Base handoff DEV-56 `838722ac3f3a41db28d6b8b4572f36d1787135ae`; branch and Slack checked before accepting DEV-57 claim. Production operator-confirmed KiCom version remains **0.9.31 healthy** and private Engram inactive. No live server changes, credentials, private memories or production installations were made in this work segment.

## Actual deliverable, parent/provenance and independent verification

Built a NEW full native package, leaving the original DEV-55 0.9.32 candidate and actual live 0.9.31 untouched. The package is stored in the current ChatGPT conversation under `/mnt/data/mirage-dev57/KiCom-0.9.32-Mirage-OAuth-MCP-STABLE-ISSUER-PREPARED.zip`, SHA-256 **`2e572d70d0fb9de50425e581a5c84aefc6e84aec962eef858e45d77bdc9e0503`**, 263673 bytes, 69 unique ZIP entries. Exact parent full 0.9.31 ZIP: `/mnt/data/Mirage-Engram-0.9.31/KiCom-0.9.31-Mirage-MCP-Route-INACTIVE.zip`, SHA-256 `9ad279b4bd084b13b8eba9327851440721c78f2894d1664d017fb72be21591c3`. This is direct 0.9.31→0.9.32 genome lineage. `modules/dev/PasskeyBridge.php`, original guardian/recovery/index/living are byte-for-byte parent originals. No private data or keys bundled; new OAuth PHP modules are default disabled. The older DEV-55 candidate SHA `298336e74ba91606c924eb67aa6e107799efc94ff6d9524f8bd648ce912eec53` is **superseded**; never install both 0.9.32 candidates sequentially.

Downloaded exact latest OAuth source export via GitHub Actions: run **35594240289**, tested source/workflow commit `cd96d9c1b2906158b32835bad2c7e7b212f0ed6e`, artifact ID `10635773318`, source ZIP SHA256 `2ba544189b71542a7ff046f8e8cb983e28290081f0d597a0bf68dd93685748b2`. All six individual PHP/JS SHA256 source manifest entries checked and independently verified byte-identical to the respective files inside the COMPLETE 0.9.32 artifact. This version now actually includes DEV-56's stable ChatGPT OAuth callback, RFC-9207 `iss` response, exact server-pinned client/redirect metadata, and explicit one-use consent cancellation.

Executed local `build_native_0932.py`, `verify_native_0932.py`, `test_native_0932_http.py` from current conversation's `/mnt/data/mirage-dev57` (these scripts themselves are NOT in GitHub yet). Native verification log confirms the ORIGINAL KiCom-0.9.31 self-update verifier ACCEPTED the exact final package with direct parent/generation and explicit human-admin RED risk. **65/65 native tests PASSED**, 50 PHP lints/1 JS lint, fresh manifest, unchanged security kernel, and 3 intentionally corrupted packages rejected. Local TCP HTTP test against extracted exact final ZIP with SYNTHETIC HTTPS/Apache rewrite reports **14/14 PASS**: real default remains OFF/404 with no private DB creation; synthetic metadata now advertises RFC9207; fresh browser original password login leads to original admin session and no token is issued from password alone. A separate source-to-ZIP byte matching pass confirmed all changed OAuth components. The isolated local PHP CLI lacks PDO SQLite, so the exact final ZIP was **not** exercised here with a positive end-to-end HTTP→OAuth→SQLite test. Such source-level tests were independently green previously (DEV-54 26/26 and DEV-56 38/38), NOT on the actual All-inkl server or with an independent ChatGPT instance.

Reusable local source/build/test bundle: `/mnt/data/mirage-dev57/Mirage-DEV57-Quellen-und-Pruefung.zip`, SHA256 `28c474b93995c3fabb173801facbda838e52308048ddb0e3b8064e55c97d3de4`. Includes tested scripts, logs, exact source export and SHA manifest. Operator-safe local status file: `/mnt/data/mirage-dev57/INSTALLATION-STATUS-DEV57.md`. **No final binary release or native ZIP uploaded into GitHub or KiCom auto-update feed**; source-only CI and the exact source references live in GitHub. Do not invent a sandbox path if these conversation files are absent in a future session.

## Blocking boundaries, no false victory

No direct Opera browser connection or All-inkl privileged host access; no real vhost/alias/PHP UID/sibling access and backup+restore attestation. Real `oauth_client`/iss mode must be operator-pinned; stable ChatGPT callback is DOCUMENTED, but actual Work plugin registration and login need to be performed and confirmed within the user's permitted app-connection flow, not assumed from an OAuth source test. The underlying private OAuth/activation DBs and any production personal-memory activation require a specifically approved real-host deployment. Operator must explicitly authorize any 0.9.32 native production install and subsequent separate private runtime activation; the present candidate stays OFF by default. No real independent ChatGPT recall has been performed. Do not ask Christoph to redo the confirmed original 0.9.29 live SQLite roundtrip or passkey enrollment.

**Next action:** use authorized host-capable connector/browse access if available to inspect vhost/UID/open_basedir/backup and actual private owner/host schema without leaking secrets. Then obtain an explicit operator install authorization for the VERIFIED complete native 0.9.32 candidate, run its real inactive HTTP smoke test, perform separately approved synthetic-only activation under verified isolation, connect a real ChatGPT Work plugin and demonstrate independent fresh-chat recall. If this host access is unavailable, continue developing a complete future self-test/restore procedure, not an unrelated new scaffold or unverified status report.
