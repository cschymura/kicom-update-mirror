# MIRAGE DEV-92 — original first-party api.php -> real private write/update/archive service

2026-09-23. **DEVELOPMENT ONLY. No production installation, no valid full native ZIP, no live OAuth/write/ChatGPT end-to-end result.**

## Concrete working integration (not another abstract operator)
- Previous DEV-91 original SHA-verified KiCom 0.9.37 native MCP host now supports a server-only opt-in writer. DEV-92 implements the missing **first-party writer factory**: `dev92/KiComEngramNativeWriteFactory.php`. It receives ONLY the bearer/combined identity/private approval ALREADY checked by the actual original native host; checks the original pinned web/private directory, refuses missing preexisting private `engrams.sqlite` or 0600 fixed-name signing-key file, opens the existing canonical SQLite **without DDL**, and composes the actual DEV-72 signed grant + DEV-86/87/88/89 canonical writer, first-party owner-bound private OAuth consent reader, durable revision/nonce/audit store and three write/update/archive operations.
- Earlier DEV-72 grant validation REQUIRED owner AND namespace strings of length >=8; the ORIGINAL productive namespace is `project` (7 characters). This meant ALL production native writes would fail even with a valid approval. Corrected the grant's owner/namespace validation to the actual original canonical KiCom identity pattern (1–64 characters), preserving original longer requirements for grant_id, token fingerprint, connector and nonce.
- A new grant and short-lived HMAC signature are created SERVER-SIDE from the already approved OAuth consent for each actual request. **No new Passkey/manual approval per remembrance.** Nonce is deterministic HMAC of original access-token hash + requested operation + idempotency key, so a retried identical operation processed by a different PHP worker retains the same durable nonce and returns the original SQLite receipt; changed content or a revoked first-party approval is denied. No client-supplied grant, owner, namespace, provenance or signing key accepted.
- `dev92/native-original-api-first-party-write-factory.patch`: ACTUAL original `api.php` entrypoint receives server-internal write factory injection **ONLY when operator-owned `engram_write_enabled=true`**. Defaults to null so the previously working original read-only route is unchanged and no write tools are exposed. The original DEV-91 host still separately validates combined OAuth, current owner/passkey and first-party consent. This native entrypoint patch was applied in a LOCAL exact-parent DEV-91 staging api.php and its PHP syntax passed. Full native module tree/package composition is still pending; the committed patch is NOT itself an installable package.

## Independently verified actual integration tests
GitHub Actions workflow run **35837191751**, job **107103355603**, result SUCCESS; PHP 8.2 real `pdo_sqlite`, full existing synthetic suites plus new private on-disk separate-connection first-party write factory suite:
- DEV92 first-party write/update/archive, stable nonce across two factory/SQLite connections, revoke/readonly/no private signing key and unchanged canonical history: 15/15.
- DEV89 private consent: 20/20; DEV88 provenance: 17/17; DEV87 private schema: 13/13; DEV76 canonical write: 10/10; DEV86 cross-connection write grant: 18/18.
- **93/93 synthetic genuine PDO SQLite assertions passed, 0 failures; 3 PHP syntax checks passed.**
- Local original-native api.php opt-in patch syntax and default-off gate: passed; NOT an on-host HTTP/OAuth/Passkey or original native MCP network end-to-end test.
Run URL: https://github.com/cschymura/kicom-update-mirror/actions/runs/35837191751

## Exact native source-map for ONE future combined package
When assembling one complete release from the exact parent archive, copy the tested DEV92 factory and dependent DEV72/75/76/87/88/89 sources into `modules/engram/`, rewrite ONLY the DEV source-tree-relative imports (`../devXX/<Class>.php`) to same-directory native require_once; preserve hashes of all original modules not explicitly changed. Existing native DEV91 bridge requires DEV91 `KiComEngramNativeMcpWriteGate.php`. Then apply DEV90/91/92 native entrypoint/MCP patches ONCE, merge with the earlier DEV82/83/84/85 OAuth continuity modifications and verify entire tree. Staged `/mnt/data/mirage-dev92-stage/api.php` and the original exact 0.9.37 ZIP may exist only in this session; a future run must independently establish availability and SHA.

## Remaining REAL release blockers
1. Original KiCom 0.9.37 admin/passkey OAuth code issuance is still READ-ONLY. There is NO first-party separately approved combined-scope/write-code issuer and no production private consent table provisioning or revocation UI. The DEV92 factory is intentionally DEFAULT-OFF and inaccessible with the old read bearer. Integrate true first-party write-scope approval/issuer with current passkey and additive operator-only private schema/backup before enabling.
2. Native read refresh/ui_locales fixes DEV82/83/84/85 must be merged into THE SAME exact-parent native tree and tested with actual PHP PDO SQLite/HTTP. Preserve old `engram.read` and existing prior successful independent synthetic MCP retrieval.
3. Native HTTP MCP full network tests, version/manifest/Genome/installer/recovery/rollback of ONE whole candidate. Optional FTS5 default-off only if positively tested. No assumptions about ChatGPT platform refreshing tokens or suppressing its own prompts.
4. Christoph separately authorizes ONE production installation and ONE real new-chat/iPhone OAuth trial. If it fails, automatic auth/security redesign STOPS; perform shared evidence-based developer review with Christoph.

No productive KiCom files, private records, real credentials, consent status or tokens were changed. No additional manual step for Christoph at this development point.
