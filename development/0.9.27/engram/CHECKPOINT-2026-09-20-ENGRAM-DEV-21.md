# KiCom Engram — DEV-21 actual identity-scoped memory write/retrieve roundtrip (synthetic DEV; NOT live)

Date: 2026-09-20. Christoph's immediate requirement is **a usable persistent Engram**, not another handwerker bootstrap or repeated failed read loops. The existing iPhone time trigger works as an activator; do not ask Christoph to recreate automations or repeatedly run terminal commands. Public mirror contains ONLY source code, synthetic fixtures and test metadata, never real user memories, chat text, private local/server paths, secrets, keys or backup bytes.

## Tangible new executable implementation

Added `development/0.9.27/engram/KiComEngramAuthenticatedBridge.php`. This is a self-contained, **non-networked DEV bridge** over the existing private SQLite `KiComEngramStore` and the existing `KiComEngramIngestionGate`. It implements:
- `remember(entry)`: requires a trusted server-supplied, verified subject, exact allowed namespace, explicit `engram.write` right and an exact-record one-time trusted consent callback; reuses the existing ingestion secret-screening/provenance validation. Caller cannot inject a different subject or permissions in the entry. The verified identity is pinned for the individual operation to avoid TOCTOU between authorization and actual insertion.
- `recall({namespace,query,limit})`: bounded, scoped, only returns current verified subject's selected records with provenance and revision metadata. Requires trusted server-supplied `engram.read` permission, exact namespace and limit ≤10; rejects unknown client keys, including a caller-supplied subject. There is no public HTTP endpoint, public route or authentication implementation.
- Separate newly constructed bridge/store instance can reopen the same private SQLite file and retrieve the original synthetically written record **without any shared chat history**.

Added `test-engram-authenticated-bridge.php` to the existing Engram regression suite and fixed-list, non-installable review source package. Checks include actual approved synthetic write, selective read, independent instance/process-style reopen of committed SQLite data, cross-subject and namespace denial, fake subject fields, excessive/incomplete query, replay of consumed consent, read-only and write-only scopes, unverified identity denial and read-back after rejected requests. 17 **new synthetic bridge assertions**; do not conflate them with a real user-memory ingestion, authenticated online connector or production instance.

Code commit containing bridge, regression hook and updated review package: `1d33ab6519fd447320ae4ccfb22cdb867f6f6794`.
GitHub Actions: `KiCom private Engram DEV` run **35522050174**, job **106107700022**, result **success** on that exact code commit. Logs contain `KICOM_ENGRAM_AUTHENTICATED_BRIDGE_TESTS_PASSED=17`, explicit PASS for independent reopen and cross-subject denial, original R3 source/package preflight, native KiCom review verifier `ENGRAM_STAGING_REVIEW_VERIFIER_OK=1`, classification `red`, unchanged kernel, `ENGRAM_STAGING_REVIEW_INSTALL_ATTEMPTED=0`. Fixed-list review artifact contains `source/KiComEngramAuthenticatedBridge.php` and `tests/test-engram-authenticated-bridge.php`; review package is NOT deployable and existing host-probe-only 0.9.27 candidate intentionally still has memory ingestion DISABLED.

## What is independently established vs absent

Established in free public synthetic CI: user-approved record can be persisted to SQLite and recovered by a fresh bridge instance with a server-supplied synthetic identity. An unverified or differently scoped synthetic actor cannot retrieve the record through this bridge. This is a **functional memory core**, beyond a design-only description.

NOT established: any LIVE KiCom/All-inkl private memory write or retrieval; a trusted runtime adapter deriving identity from genuine KiCom authentication, production consent receipt issuer, private host config and verified same-UID cross-app isolation, protected update transaction/health/rollback, and an authorized connector by which an independently opened ChatGPT conversation reads that live private store. No real private memory is stored by this change. Do not claim the review-only ZIP enables live memory.

## Next implementation target (not another diagnostic loop)

Build and test a minimal protected KiCom DEV read/write route ADAPTER that calls this bridge only after EXISTING KiCom server-side session/subject binding, scope checks and a one-time reviewed consent issuer. Keep request data (including Slack/GitHub/other app content) strictly outside authority. Prepare a **single reviewed, rollback-capable integrated package** once real private server config/host isolation can be proven through an authorized synthetic host test. Live protected installation is a separate human decision under the original update gates; do not publish runtime private config, real engrams or identities in public GitHub or Slack. Avoid redoing already verified Mac/iPhone/external encrypted synthetic backup steps.

If a scheduled Handwerker cannot read Slack or live BOOTSTRAP follow-up resources, it may continue a conflict-free isolated synthetic implementation using the **exact known GitHub files in this checkpoint**; do NOT run nine identical failed resource reads, give a generic OFFLINE_ERGEBNIS, or require Christoph to shuttle a report. Write concrete tested code/checkpoint through the available authorized GitHub connector, otherwise state precisely which real tool boundary prevents write. Do not invent a live result.
