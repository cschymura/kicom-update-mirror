# Mirage Engram DEV-34 — geschützter Admin-Einrichtungs-Prototyp

Stand 2026-09-20. Christoph meldete KiCom 0.9.27 installiert. Ein FTP-Screenshot zeigt das Geschwisterverzeichnis `engram-private` (0700) mit `data` und `backups` (0700), fehlender `engram-host.json` und laut Dateimanager leerem `data`; dies belegt keine tatsächliche PHP-UID-/VHost-Isolation und keine reale Server-Datenbankfunktion.

Neu, **ausschließlich lokal im aktuellen Chat**: `KiComEngramSetupWizard.php` und `patch_admin.py` ergänzen in einer lokalen, entpackten 0.9.27-DEV-Kopie die bestehende Admin-Session, CSRF und HTTPS um `admin.php?engram_setup=1`. GET ist read-only; bestätigter POST erzeugt nur fehlende private 0700-Verzeichnisse, eine leere Owner-Registry (0600) und `engram-host.json` (0600) mit allen betrieblichen Flags `false`, `runtime_source=setup-pending`, `private_memory_scope=setup-pending` und ohne Benutzerzuordnung. Keine SQLite-Initialisierung, Freigabe, Aktivierung, Überschreibung oder eigenständige öffentlich nutzbare Installationsroute.

Tatsächlich geprüft: PHP-Syntax von Admin-Patch und Setup-Modul sowie **19/19 lokale synthetische Positiv-/Negativtests** (ungültige Session/CSRF/HTTP, Dateirechte, deaktivierter Betrieb, Wiederholung, Symlink). Das lokale PHP besitzt **kein pdo_sqlite**; daher kein echter SQLite-Roundtrip. Keinerlei Live-KiCom-Änderung. Kein gültiges natives 0.9.28-Update gebaut, keine CI ausgeführt, kein realer Host-End-to-End-Test.

Das DEV-Quellarchiv `Mirage-Engram-DEV34-Admin-Einrichtung-DEV.zip` liegt nur als aktuelles Chat-Artefakt vor, SHA-256 `1097305695f308770357ade8826a66df06d00fe84f1d640d65bc2ce4c422a459`. **Nicht** direkt in den Self-Updater hochladen oder per FTP ins Live-Webroot kopieren. Kein öffentlicher Speicherort für private Erinnerungen, Originalkonfigurationen oder Keys.

Nächster Schritt: Authentifizierten Admin-Quellpatch in einem neuen, vollständigen nativen KiCom-Release mit Manifest-/Genome-/Rollback-Prüfung integrieren. Reale Serverprüfung, Passkey-/Owner-Zuordnung, Betreiberfreigabe, Freischaltung und künstlicher echter Server-Speicher-/Abruf-Test bleiben gesondert erforderlich. Ein MCP-Server folgt erst auf den nachgewiesenen echten Engram-Speicher.
