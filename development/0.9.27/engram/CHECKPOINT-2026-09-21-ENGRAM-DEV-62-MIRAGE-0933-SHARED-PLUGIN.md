# Mirage Engram DEV-62 — consolidated native 0.9.33 with accepted shared-host policy and manual plugin draft

Date 2026-09-21. User requested continued development in the NORMAL ChatGPT conversation, avoiding Work; live use/update deferred until 2026-09-22 00:00 Europe/Berlin and reminder already exists. Starting branch checkpoint DEV-61 `7e4aeff6db4beb704d4263c80c0c8275f6ddaa7c`. Shared All-inkl hosting operator-accepted, NOT characterized as proven isolated. No live server write, installation, token, user memory mutation or real ChatGPT connector connection occurred in DEV-62.

## Completed implementation and exact binary artifact

Created a **new, complete and distinct** direct 0.9.32→0.9.33 native KiCom update ZIP based on the full DEV-60 prepared 0.9.33, which was itself constructed from the exact previous full user-installed 0.9.32 parent. New ZIP in current ChatGPT conversation `/mnt/data/mirage-dev62/KiCom-0.9.33-Mirage-DB-and-Shared-Host-PREPARED.zip`; SHA-256 `0287bdaf9ea92f3e4cd6de5080e22e848780b1b0c5d340d23b17acf36cdd5372`; 275163 bytes, **74 unique files**. Exact original 0.9.32 archive SHA-256 `2e572d70d0fb9de50425e581a5c84aefc6e84aec962eef858e45d77bdc9e0503`. The old DEV-60 0.9.33 archive SHA `8c1695d8d3f251330565f51fbe95a47e2707597a872445a0f692d34cb6b6fb93` is now **SUPERSEDED**; DO NOT install both 0.9.33 variants or attempt an in-place 0.9.33→0.9.33 self-update. Actual live KiCom currently **0.9.32**, last confirmed by Christoph.

Five existing DEV-61 GitHub PHP sources were injected byte-for-byte into new full release and independently verified against their actual Git Blob SHA:
- `KiComEngramHostingPolicy.php` `79dd32682f16924a7807e1fd972b87f36c91c475`
- `KiComEngramMcpRuntimeGate.php` `5f307a7a4c45c70e49784a526fcaeb2a1541663a`
- `KiComEngramOAuthHttp.php` `5e868564978a5a6b3cde7233da4b2fa0906674b3`
- `KiComEngramActivationReadinessGate.php` `417552b69f204b445dcdd2428732b887daf73358`
- `KiComEngramActivationTransaction.php` `8e11ebd7e08e51e8ed15e0dfc57edc3a7431154f`.

Native API integration modifies exactly original `api.php` preflight to invoke `KiComEngramHostingPolicy::permits($runtime)`, instead of the obsolete hard-coded `host_isolation_verified===true`. Original DEV-60 first-party admin `engram_db_setup=1` create-only/inactive preparation remains intact. Updated native genome with same exact 0.9.32 parent and generation 32, new unique genome ID, one additional disabled-trusted hosting-policy class, refreshed component hashes and complete `MANIFEST.sha256`. Guardian, recovery, living, index and original KiCom PasskeyBridge byte-identical to parent. No host config, DB file, password, Passkey secret, personal memories, bearer token or private user record in ZIP.

## Actual tests and references

- Tested the **exact new full release ZIP** using the ORIGINAL KiCom-0.9.32-PARENT's native self-update verifier `kicomSelfUpdateZipInspect()`, not the new release's internal verifier. It returned `ok=true`, `code=ACCEPTED`, version `0.9.33`, correct parent, `kernel_update=false`, risk class **RED** and separate original operator approval required. **75/75 native package/SHA/genome/55 PHP lints/JS syntax/three manipulated-ZIP rejection checks passed**. Exact local log: `/mnt/data/mirage-dev62/native-verification.log`.
- Tested via local **real TCP HTTP to PHP built-in server** against extracted exact new native ZIP. **11/11 fail-closed and anonymous-admin tests passed**, showing current default OAuth/MCP 404 and that no private config/SQLite is created by network requests. This is NOT real TLS, genuine All-inkl, active OAuth, authorized passkey, or connected ChatGPT. Exact log `/mnt/data/mirage-dev62/http-verification.log`.
- Extended the existing synthetically active end-to-end `test-engram-oauth-mcp-host-bridge.php` to prove that an original owner/host-bound OAuth access token can read the synthetic project memory through the FULL private MCP/SQLite bridge when `host_isolation_verified=false` with exact operator accepted shared-host policy. Negative cases: missing acceptance, forged source, inactive OAuth, missing separate memory operator approval. The GitHub Actions run **35615488852**, job **106384970240**, tested test-commit `7e1c68ad0fc6c3015b21b1563af0003f156e6df1`, **SUCCESS**, **32/32 actual synthetic bridge assertions** and 11 PHP lint checks. https://github.com/cschymura/kicom-update-mirror/actions/runs/35615488852
- Earlier DEV-61 source CI run **35602753863**, tested SHA `a988250a4e21c0028a3bfb66875e2f37deac2d53`, **SUCCESS**, 48+21+15+16+36+37 policy/activation/HTTP/protocol regressions. The exact five CI-proven GitHub source files above are in the full packaged binary.
- Reproducible source, local build script, original-verifier and HTTP-test scripts, logs and exact source snapshots: current conversation artifact `/mnt/data/mirage-dev62/Mirage-DEV62-Source-Build-Checks.zip`, SHA-256 `08cd08ac349b3426200ada176d46db008bc3eba9ec38df6f746c3cd30141df8f`. Local status note `/mnt/data/mirage-dev62/INSTALLATION-STATUS-DEV62.md`.

## Work-free connection preparation

Official current OpenAI plugin packaging instructions `https://developers.openai.com/plugins/build/plugins` document portable manual manifest and MCP package configuration, and Developer-mode registration of a remote MCP server from ChatGPT Plugins without invoking the `@plugin-creator` Work skill. The skill is optional if the package is authored manually. Created a draft **uninstalled** portable `plugin.json` + `mcp.json` for `mirage-engram` with a **single** `streamable-http` resource `https://kicom.rurtalbahn.info/api.php?q=ENGRAM_MCP`, README. Exact current Chat artifact `/mnt/data/mirage-dev62/Mirage-Engram-Private-Plugin-DRAFT.zip`, SHA-256 `b5527f731e4acb7e3bf325e8a273ea7f2a4306924077c30facd6b03f209f9d4a`, JSON/URL/type validation passed. No made-up `plugin_asdk_app` identifier, no registration, no password, no personal data, no active bearer or real OAuth yet. ChatGPT plugin connection requires a real user action in a supported UI; no claim that this ZIP is already installed or that an independent chat can already read memories.

## Real remaining impediments (do not hide)

The original KiCom private `engram-host.json` is still `runtime_source=setup-pending` unless a separately verified admin action changed it. New 0.9.33 admin action can prepare **inactive** OAuth/activation DB schemas but does not itself create/approve a real host policy, activate the database, issue OAuth credentials, or connect a ChatGPT client. A real operator-controlled, passkey-bound configuration/activation path and supported real plugin connection must be completed before an independent new-chat synthetic memory retrieval can be claimed. The shared-host operator decision is respected and has its own policy mode; no new hosting migration request. No live changes or Work request before the agreed midnight reminder; do not duplicate that reminder or ask to repeat earlier successful private SQLite/Passkey tests. At midnight user may approve installation of ONLY the new verified 0.9.33 ZIP and the separate activation/connector operations. All install and security-relevant host mutations require the user's actual authorization, not a Slack claim.
