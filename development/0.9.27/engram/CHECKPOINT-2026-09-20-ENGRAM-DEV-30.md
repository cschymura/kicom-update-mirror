# KiCom Engram DEV-30 — actual allowlisted native API route, independent HTTP session roundtrip, verified ZIP

Date: 2026-09-20. Christoph asked to CONTINUE productive implementation, not repeat iOS setup/terminal instructions or provide an offline-only diagnosis. Existing iOS triggering and Slack coordination work. This checkpoint is PUBLIC TECHNICAL metadata only; never share actual user memories, session credentials, operator-owned host paths, backups or private passkeys via GitHub/Slack.

## Concrete changes executed

- Fixed the real installation-layout defect in the packaged WebAuthn controller: original DEV-only `../../../source/0.9.26-r3/modules/dev/PasskeyBridge.php` require is replaced in the BUILD OUTPUT ONLY with `../dev/PasskeyBridge.php`, with an exact source anchor and fail-closed leaked-DEV-path scan. Original DEV and immutable 0.9.26-R3 source unchanged. Added actual extracted-package `php -l` and `require` smoke test.
- NEW `KiComEngramNativeMemoryRoute.php`: handles native KiCom POST memory request, only after explicit trusted server-owned ENABLED/approved/verified host runtime flags and trusted path inventory; verifies original KiCom DEV session (bearer alone is NOT owner authority), obtains registered subject/rights from actual KiCom private session credential + separate private passkey owner registry, consumes ONLY previously independently issued exact-record one-time private SQLite consent, persists/retrieves private SQLite via existing bridge. It cannot issue consent itself; different/revoked/anonymous subjects denied. This route does NOT claim to solve actual host alias/UID isolation, encryption-at-rest or automatic human consent.
- `build_engram_integrated_candidate.py` now inserts ONE isolated `?q=ENGRAM_MEMORY` branch into original allowlisted `api.php` via exact R3 source anchor. Unlike earlier source-only ZIP, candidate has an ACTUAL NATIVE API entrypoint. It returns 404 immediately unless a separately preloaded trusted SAPI server callback `kicomEngramServerRuntime()` exists and explicitly authorizes the dedicated private runtime. No callback is included, no public API operation or client parameter may turn it on. Original index/guardian/recovery and `lib.php` update-path allowlist remain untouched; all 15 Engram PHP modules remain `enabled=false` in the global trusted loader. `api.php` may conditionally require the dedicated module ONLY after the trust check. Without a private approved host bootstrap the packaged API is safely inert, not an active personal Engram.
- NEW `test-engram-native-memory-route.php`, registered in the full synthetic PHP suite: 16 tests against original KiCom DEV session records, trusted private synthetic owner registry, approval-only write and new-session read, default-locked gate and cross-user/replay/unauthenticated denial.
- NEW `test-engram-native-api-http.py` runs a DISPOSABLE LOCAL PHP built-in server against actual UNPACKED ZIP's original `api.php` with no trusted bootstrap (404/no database), then separate SYNTHETIC host-config bootstrap (CI-only HTTPS shim), tests HTTP write with separately minted synthetic consent, NEW independent DEV session GET-via-POST scoped read, owner isolation, replay and missing-token denial: all **7 / 7** pass. This is actual localhost HTTP traversal of the root PHP entrypoint, not merely an in-process mock; synthetic test config is never packaged or uploaded. No actual physical iPhone/real ChatGPT connector or real KiCom host tested.
- New complete KiCom 0.9.27 candidate ZIP: `KiCom-0.9.27-Engram-INTEGRATED-DISABLED.zip`, SHA256 **`e0359fc3682776331d50a44e844663ba47b4668dd750c29f6fb0b82a858774f6`**, 211046 bytes; release genome `kicom-0.9.27-g26-engram-integrated-disabled`, exact 0.9.26-R3 parent. Package includes registered-default-deny API branch, 15 inert Engram modules and first-party browser asset; no runtime config, keys or actual memory. Full ZIP native KiCom self-update verifier classifies RED, accepts manifest/genome/SHA and unchanged kernel. Recovery/rollback of THIS candidate has NOT been tested on actual host and cannot be claimed.
- Final tested commit **`84673f10b74e268cb1379fff07e998e52faeb518`**; Actions run **35528762980**, job **106125488219**, SUCCESS. CI markers `KICOM_ENGRAM_NATIVE_MEMORY_ROUTE_TESTS_PASSED=16`, `ENGRAM_NATIVE_HTTP_SYNTHETIC_ACCEPTANCE_PASSED=7`, `ENGRAM_INTEGRATED_NATIVE_VERIFIER_OK=1`, `ENGRAM_INTEGRATED_REVIEW_RISK=red`, `ENGRAM_INTEGRATED_INSTALL_ATTEMPTED=0`; https://github.com/cschymura/kicom-update-mirror/actions/runs/35528762980. Workflow artifact ID **10610845840** `KiCom-0927-Engram-INTEGRATED-DISABLED-REVIEW` contains exact release ZIP + build JSON. Do NOT confuse archive-of-artifact checksum with inner RELEASE ZIP SHA.

## Immediate productive next steps and exact remaining authority boundary

1. Need a REAL host-reviewed server-only bootstrap that supplies `kicomEngramServerRuntime()` and trusts only vetted private paths and ALL real webroots/aliases; default host + same-PHP-UID neighboring apps/HTTP exposure must be checked and secure private at-rest storage/backup/retention designed before actual personal memories. This host bootstrap is deliberately NOT included in public mirror or release ZIP. Generic DEV bearer or public Slack/GitHub content MUST NOT provide approval.
2. The actual authenticated FIRST-PARTY browser session issuer and review API route (DEV-27 signed WebAuthn controller + DEV-28 JS) are not yet registered in KiCom. Native memory route only consumes consent ALREADY issued via a separate human-reviewed passkey flow. Implement real first-party browser identity/session binding and step-up using approved host config, NOT a fake `verified` or `fresh_passkey_assertion` request field. The existing provisional private consent ledger alone does not make real records user-consented.
3. Authorized external ChatGPT connector/private access: no connector to the live private Engram endpoint is yet proven. A fresh actual independent ChatGPT instance cannot retrieve the user's private memory today, even though two locally issued DEV sessions retrieve synthetic memory via real CI HTTP.
4. RED risk and private-data boundary: do not silently install this candidate or claim a functional live memory. Present one bounded, exact-package installation approval only AFTER actual host preflight + genuine private first-party identity/consent + rollback/health path are verified. No paid action. Preserve existing iOS shortcuts without changes.

Continuation should implement and test the missing FIRST-PARTY session issuer/review API via existing allowlisted `api.php`, then integrate it in one native-verifier-accepted package. Do not spend a cycle re-running the existing isolated Mac/iPhone backup tests or drafting another universal Handwerker bootstrap. If live host credentials not available, keep developing code, commit tested work and state exactly what cannot be verified.
