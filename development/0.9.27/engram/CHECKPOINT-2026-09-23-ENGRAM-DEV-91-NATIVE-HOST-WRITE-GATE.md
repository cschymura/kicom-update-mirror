# Mirage DEV-91 — FIRST-PARTY native MCP host scope separation and private write-capability gate

Date 2026-09-23. **Development only; NO installable package, NO production upload, NO private DB change, NO OAuth permission issuance.**

## Concrete native source progress beyond DEV-90
Verified exact original KiCom 0.9.37 parent archive SHA256 `0c6e02c64d44d603cb229f562d189f1b798bd78c2188fd2907e6b5cbafdb5ea7`. The ORIGINAL (before DEV90) native `KiComEngramOAuthTransactions.php` SHA256 is `a7b323006e6773325714084c8cb50d843fff24d85da23876feeac3e7c5fc380f`. The existing DEV90 original-native MCP protocol now exposes three write tools ONLY when a trusted host passes an opt-in writer callback; the ORIGINAL 0.9.37 host passes no such callback, so it remains strictly read-only.

This DEV91 adds **native host-side conditions** rather than merely trusting a callback:
- `dev91/native-0937-separate-combined-scope-verify.patch` adds `KiComEngramOAuthTransactions::verifyCombinedScope`, a separate strict validator for the EXACT `engram.read engram.write` token row. Existing `verify` for original `engram.read` remains byte-for-byte unchanged. Write-only and old read-only tokens cannot masquerade as combined. It supplies the same original five-key identity expected by the existing owner/activation runtime gate. A token with combined scope is NOT by itself an authorization to write.
- `dev91/KiComEngramNativeMcpWriteGate.php` checks the current original FIRST-PARTY owner/passkey registry (owner enabled, exact project namespace, currently assigned read+write rights), exact resource/host/connector/owner/token hash, current non-revoked and unexpired combined OAuth token, and a single separate current owner-bound private WRITE consent record. It performs READ-ONLY SQL and NEVER issues a permission or creates DB tables. Absent/incomplete/revoked consent denies write.
- `dev91/native-0937-host-gated-write-dispatch.patch` integrates this into the actual ORIGINAL native `KiComEngramOAuthMcpHostBridge::handle`, with an optional FIRST-PARTY `$firstPartyWriteFactory`. Without this server-only injection, existing read remains unchanged and no write tools are advertised. Even with this injection, original host only passes a write handler to the native DEV90 protocol AFTER separate combined OAuth verification and the live private owner+consent gate. The original runtime/activation check is still performed by the native MCP protocol itself. This is a STAGING patch, not installed; the original `api.php` does NOT yet supply a trusted factory.
- `dev91/test_native_scope_separation.php` is a test-double regression of the EXACT staged native PHP original OAuthTransactions class; it requires `KICOM_DEV91_NATIVE_FILE` set to the exact staged class file and cannot run as-is from GitHub without the original parent source being materialized.

## Independently verified tests, with correct boundaries
- Local PHP 8.4 EXACT original-native scope method test, **11/11 PASSED**, using PDO test doubles; the original legacy read-token verifier was unchanged. This is NOT a real OAuth authorization-code, passkey or private SQLite test.
- Local Python sqlite3 ran **10/10** synthetic positive/negative SQL checks using the EXACT SELECT SQL extracted from the committed gate source, including read-only token, revocation, host/namespace/fingerprint, expiry and future approval. This is SQLite C-engine SQL REPLAY, not PHP PDO.
- GitHub Actions run **35835940376**, job **107099296537**, completed SUCCESS with actual PHP 8.2 pdo_sqlite: **16/16 native write-gate SQL/owner/passkey/consent synthetic assertions, 0 failed**. URL: https://github.com/cschymura/kicom-update-mirror/actions/runs/35835940376 . This tests the committed DEV91 gate against an in-memory SQLite fixture, NOT a deployed host.
- Local whole-source staging from the exact 0.9.37 parent plus DEV90 protocol, DEV91 native scope verifier, DEV91 native host bridge and new gate: **63/63 PHP syntax checks passed**; original+optional-writer native protocol test **12/12 passed**, including old read continuity and denial of write on the unmodified original host. No full installer/Genome/recovery/rollback result is claimed.

## The honest release blockers — DO NOT announce READY_FOR_RELEASE
1. Existing original native OAuth admin, PasskeyConsent, AuthorizationCode exchange, protected-resource discovery and OAuthTransactions::verify are READ-ONLY. Genuine first-party, owner-bound, separately approved `engram.write` authorization-code/token issuance, durable revocable consent-row creation and original owner registry assignment are STILL NOT integrated. This is the next essential actual code task.
2. The existing original `api.php` still invokes the original host without a writer factory. This MUST be changed only when the factory actually constructs the DEV89 private-consent-bound DEV86/87/88 canonical mutation service from server-proven owner/namespace/consent, AND issues per-call signed nonce grants internally after prior operator-approved OAuth WRITE consent, so users are NOT asked to manually approve each new memory. Currently no write is available live and cannot be falsely claimed.
3. Complete DEV82/83/84/85 original-native OAuth refresh+ui_locales integration into the SAME native tree, real PHP PDO SQLite/HTTP/MCP end-to-end and exact existing private SQLite migration/backup before full package. Refresh client support on actual ChatGPT platform remains unproven.
4. Confirm native write provenance against current private consent; remove all synthetic labels from production mutations; preserve the existing proven read-only engram_search. Then original manifest/genome/updater/recovery/rollback and full package verification; ONLY ONE complete candidate for Christoph's separate installation approval, ONE genuine new-chat/iPhone live auth test; on another failure STOP automated OAuth rewrites and review with Christoph.
5. No personal memories, production tokens, private SQLite content or credential material were transferred to GitHub. Synthetic tests use only fabricated values.

Branch `work/kicom-engram-dev91-native-scope-separation` is the currently committed authoritative next development point. The local exact native staged tree at `/mnt/data/mirage-dev91-full` is a WORKSPACE copy, NOT a persistent GitHub artifact or an installable ZIP; verify its existence in every new chat/runtime rather than assuming it is available.
