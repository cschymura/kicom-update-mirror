# KiCom Engram — DEV-29: real update ZIP with integrated source, native-verifier accepted, memory DISABLED

Date 2026-09-20. Christoph requested action ("mach") toward the full KiCom/Engram installer; his existing iOS automations, Slack transport and previous synthetic backup/restore checks must not be reconfigured or repeated. His desired finish line is a live authenticated private Engram that a genuinely independent ChatGPT conversation can write/read, not a code-only achievement. Do not claim LIVE without that test.

## Material code and downloadable file actually produced

New `development/0.9.27/engram/build_engram_integrated_candidate.py` composes a genuine, full KiCom 0.9.27 release ZIP from the *exact verified* R3 0.9.26 archive/source. It includes 14 Engram PHP memory/consent/WebAuthn/browser modules and first-party JS under allowed modules/ and assets/ directories; the existing KiCom R3 DEV hostprobe sources are hash-pinned and isolated. It advances version and genome to `kicom-0.9.27-g26-engram-integrated-disabled` with updated SHA manifest. NO private keys, memories, databases, owner registry or runtime configuration are bundled. All `modules/engram/*` are set `enabled=false` in the trusted module manifest. The original root index/api/guardian/recovery/.htaccess remain byte-identical to trusted R3. NO public Engram endpoint is registered.

Why the new root entrypoint was NOT packaged: the untouched KiCom trusted native verifier `kicomSelfUpdateInstallPathAllowed` rejects an extra `engram.php` root path with `UPDATE_PATH_NOT_ALLOWLISTED` (failed CI run 35527829803). The builder was corrected to honor that legitimate original whitelist instead of bypassing the existing security boundary. Actual first-party/API route registration will therefore require a separately reviewed narrow modification of an existing allowlisted entrypoint (likely api.php or index.php) + genuine first-party browser-session/owner/trusted private host configuration and revalidation. Do not silently weaken lib.php's update allowlist or register the public route using arbitrary DEV tokens.

New GitHub Actions `.github/workflows/test-0927-engram-integrated-candidate.yml` builds and uploads the FULL CODE-CONTAINING DISABLED 0.9.27 candidate ZIP. It checks the original R3 source/release hashes, JS syntax and complete synthetic PHP suite, runs original native `kicomSelfUpdateZipInspect` with risk RED, verifies unchanged kernel/critical source, no user-private file paths, all 14 modules present but disabled, and no public memory endpoint; it does NOT perform installation. Final CI **35527926440**, job **106123241885**, SUCCESS on SHA **`0858e65c7814b5e4b71c2213922b298b1074863a`**. Logs include `ENGRAM_INTEGRATED_NATIVE_VERIFIER_OK=1`, `ENGRAM_INTEGRATED_REVIEW_RISK=red`, `ENGRAM_INTEGRATED_MODULES_PRESENT_AND_DISABLED=1`, `ENGRAM_INTEGRATED_PUBLIC_ROUTE_UNREGISTERED=1`, `ENGRAM_INTEGRATED_REAL_MEMORY_INGESTION_ENABLED=0`, `ENGRAM_INTEGRATED_INSTALL_ATTEMPTED=0`.
GitHub run: https://github.com/cschymura/kicom-update-mirror/actions/runs/35527926440
Actions artifact ID 10609559752, name `KiCom-0927-Engram-INTEGRATED-DISABLED-REVIEW`. It contains the **actual release ZIP** `KiCom-0.9.27-Engram-INTEGRATED-DISABLED.zip` and JSON build record. Release ZIP SHA-256 `1c6276f3f6bce48eb82671fbf077e5fa409c101ab06adf0b6cd68f4378a65838`, 208612 bytes. CI artifact ZIP is a separate enclosing archive, do not confuse their digests.
The original 0.9.26 R3 code and package SHA checks passed, and no live installation, update-feed publication or user data ingress was attempted. The native updater's self-update/recovery mechanism exists, but a rollback after installing THIS exact new candidate has not been executed on the user's real host; do NOT assert an end-to-end rollback test.

## Honest remaining critical path for the next instance

This update ZIP can carry the reviewed inert Engram source through KiCom's native package checker, but it is not an active memory service and must NOT be installed as a "working Engram" or described as live. To meet Christoph's real goal, implement and independently test the trust-bound genuine first-party browser session resolver; register minimal authenticated memory and browser review route via a narrowly reviewed allowlisted original API entrypoint, with reviewed human WebAuthn/consent and explicit private owner map. Ensure private storage is outside ALL accessible vhosts/aliases, verify PHP same-UID cross-app isolation and encryption/retention/backup/rollback. Build an actually functional native-verifier-accepted release and request precise authorization before live install; carry out a synthetic real-server write and a read through an authorized connector from a *new real ChatGPT conversation* (not merely second PHP object). If host credentials/browser connector are unavailable, continue code/CI work and state the exact missing authority rather than inventing live success.

User must not be used as a manual terminal/inter-instance relay; preserve existing iOS scheduling. Share only synthetic public technical metadata in GitHub/Slack.
