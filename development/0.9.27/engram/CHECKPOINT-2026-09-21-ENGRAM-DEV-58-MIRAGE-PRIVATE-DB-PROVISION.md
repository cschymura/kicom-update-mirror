# Mirage Engram DEV-58 — inactive OAuth/activation SQLite provisioning; exact next server blocker

Date 2026-09-21; branch `work/kicom-0.9.27-pam`; preceding checkpoint DEV-57 SHA `5f851e80410753f84e874c7c62ffe20bf029d16e`. Christoph provided a screenshot of the actual All-inkl KiCom admin page confirming live update **0.9.31→0.9.32 successful**, status GESUND. This is an operator screenshot, not a host filesystem/cryptographic SHA attestation.

## Verified concrete blocker in EXACT 0.9.32 package

Inspected the current conversation's exact full 0.9.32 ZIP `/mnt/data/mirage-dev57/KiCom-0.9.32-Mirage-OAuth-MCP-STABLE-ISSUER-PREPARED.zip`, SHA256 `2e572d70d0fb9de50425e581a5c84aefc6e84aec962eef858e45d77bdc9e0503`: it contains HTTP OAuth/MCP routes but no authenticated operator action for provisioning the required private `engram-private/data/mirage-activation.sqlite` and `mirage-oauth.sqlite` schemas. The `ENGRAM_MCP` and OAuth-token routes refuse missing files and remain disabled by default. The existing setup wizard creates a config with `runtime_source='setup-pending'`; original `kicomEngramServerRuntime()` deliberately only exposes `server-only-reviewed`, hence the setup action must use a SEPARATE strictly inactive, admin-only loader, not relax the public runtime loader. Existence of the older real `engrams.sqlite` and original Passkey binding does NOT imply the two new OAuth/activation databases exist. A healthy 0.9.32 version banner proves package installation, not operational readiness.

Direct live read attempts: Opera Browser Connector reported Browser not connected; public KiCom URL failed via web tool and TinyFish (timeout), and container HTTP DNS resolution also failed. No live host credentials or authenticated All-inkl filesystem access were available. Do not claim an actual server isolation check, host OAuth state or live token issuance.

## Executable code developed and verified

Added `development/0.9.27/engram/KiComEngramInactiveDbProvisioner.php` and `test-engram-inactive-db-provisioner.php` plus CI `.github/workflows/test-0927-engram-inactive-db-provisioner.yml`. The module is a PHP class only, **NOT integrated into or installed on live KiCom 0.9.32**. It requires original KiCom admin PHP session ID and session-bound CSRF, explicit exact operator confirmation, HTTPS, existing 0700 private root/data/owners/backups and 0600 config/owner registry, and a separate `loadInactiveRuntime(__DIR__)` that reads ONLY the existing protected `setup-pending` or still inactive reviewed host JSON. It REFUSES an actually active host even if its caller fabricates an inactive policy. It creates ONLY missing `mirage-activation.sqlite` and `mirage-oauth.sqlite` in the original private data directory with initialized original DEV-40/DEV-51 schemas, using private temporary files, 0600 permissions, a restricted lockfile and rename; initial activation state remains INACTIVE, OAuth codes/tokens empty. Rerun is create-only and checks existing schemas without rewriting them. It does NOT modify existing `engrams.sqlite`, `engram-host.json`, owner registry, Passkey store, host/isolation flags, approval state or private memories. A live activation and production connector still require a separate reviewed host-isolation and signed operator approval flow.

First dedicated CI run `35595822680` FAILED: empty persistent 0600 lock file was mistakenly classified as invalid for an idempotent rerun. Fixed. A later CI `35596113526` failed because the test fixture still passed a synthetic runtime rather than the new actual config loader; test corrected to use the real exact protected JSON and assert active config cannot be overridden by client-provided inactive data. **FINAL exact tested commit `1e399809e960e647ea298c373c7c4eb89ca3f4ee`**. GitHub Actions dedicated run **35596161922**, job **106321335704**, result SUCCESS; original immutable KiCom 0.9.26-R3 release/source verification, 5 PHP lint checks and **21/21 synthetic private-database provisioning/denial/old-memory-preservation tests** passed. https://github.com/cschymura/kicom-update-mirror/actions/runs/35596161922

## Operational boundary and next deliverable

Do not imply the two private OAuth/activation schemas are already present on Christoph's host or that this class by itself makes 0.9.32 self-activating. Need integrate it into the original first-party KiCom administration as a separately approved create-only action and package it into a native follow-up release only when the full host-readiness/real-connector path is ready; stop handing out one inactive ZIP per small source component. The existing 0.9.32 installation is healthy and should not be rolled back or overwritten.

To reach actual cross-chat memory, a privileged direct host access path and real ChatGPT Work plugin registration are essential. Opera is currently NOT connected; a real app link/connection/authorization requires Christoph's action, which cannot be invented by an autonomous instance. First inspect actual host alias/vhost, PHP UID, open_basedir, sibling-app access and backup restore via a connected authorized browser/host tool. No real personal-memory activation until separate operator approval and verified compartment isolation. Do not demand that Christoph repeat already successful 0.9.29 synthetic SQLite or original enrolled Passkey tests.
