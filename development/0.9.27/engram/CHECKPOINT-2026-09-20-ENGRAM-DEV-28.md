# KiCom Engram — DEV-28: first-party WebAuthn browser client + review-page wiring; reviewed code-only package

Date: 2026-09-20. User's primary objective: a REAL private, persistent Engram across independent ChatGPT instances with automated iOS triggering. Existing iOS Shortcuts and Slack already work in demonstrated runs; do not ask to recreate 48 automations, shuttle code by hand, or repeat previous synthetic backup tests. This public repository and Slack may hold technical source, synthetic test results and handoffs ONLY: no real memories, passkeys, tokens, private paths or backups.

## Actually implemented and checked this development cycle

DEV-27 delivered an isolated server-side `KiComEngramWebAuthnApprovalController.php` and `KiComEngramWebAuthnReviewHttpAdapter.php`. The original KiCom PasskeyBridge cryptographically checks genuine P-256 signatures made by locally generated SYNTHETIC test credentials; the exact review draft, original browser session hash, CSRF, owner and seven-field content binding are matched before issuing one-use SQLite consent. The adapter handles same-origin HTTPS JSON `ENGRAM_REVIEW_BEGIN` and `ENGRAM_REVIEW_CONFIRM`; it is UNREGISTERED, needs a genuine host-side first-party browser-session resolver, and its synthetic cookie mapping in CI is not production authentication. The full signing+memory+fresh-session read test `test-engram-webauthn-approval.php` now passes **24** assertions including cross-origin denial and actual P-256 signed HTTP confirmation. This is not proof of a real enrolled iPhone passkey or live host.

DEV-28 adds two NEW first-party UI assets, not merely a design:
- `development/0.9.27/engram/engram-review-client.js`: Same-origin browser script: intercepts the exact-record review form, calls the JSON BEGIN operation, decodes base64url WebAuthn challenge/credential IDs, calls `navigator.credentials.get` with `userVerification: required`, serializes `rawId`, `clientDataJSON`, `authenticatorData`, `signature` and sends CONFIRM over same-origin credentialed POST; provides user-visible status and no raw memory/credential data in URL. If browser Passkey/WebAuthn unavailable, reports it in the UI. Browser/iOS Safari integration has NOT yet been executed: Node syntax check is NOT a browser test.
- `development/0.9.27/engram/KiComEngramBrowserReviewPage.php`: Decorates the existing server-rendered review form with fixed same-origin JSON API path, first-party JS src, aria-live status and no-JS guidance. Rejects untrusted/cross-origin API and script paths; no route registration or real user information.
- Updated `test-engram-first-party-review.php` includes same-origin HTML+JS wiring/escaping regression, now **23** PHP assertions. Fixed-list NON-INSTALLABLE code-only review ZIP includes the JS asset and browser decorator. GitHub Actions workflow now runs `node --check` on the JS in addition to the existing full PHP synthetic suites and native KiCom package verifier.

Last tested CODE SHA **`3d36ed458ab8324d8ffd313d5ef551ea93dfe4ff`**, GitHub Actions `KiCom private Engram DEV` run **35526729508**, job **106120081461**, result SUCCESS. Logs contain `KICOM_ENGRAM_FIRST_PARTY_REVIEW_TESTS_PASSED=23`, `KICOM_ENGRAM_REAL_WEBAUTHN_APPROVAL_TESTS_PASSED=24`, `source/engram-review-client.js`, `source/KiComEngramBrowserReviewPage.php`, `ENGRAM_STAGING_REVIEW_VERIFIER_OK=1` and `ENGRAM_STAGING_REVIEW_INSTALL_ATTEMPTED=0`. Link: https://github.com/cschymura/kicom-update-mirror/actions/runs/35526729508

## What this DOES NOT establish

NO registered live KiCom route; NO genuine independently authenticated first-party browser session resolver connected; NO same-origin JS files deployed at fixed live URLs; NO proven physical iOS Safari passkey interaction; NO verified same-PHP-UID host isolation/private config; NO end-to-end actual ChatGPT connector; NO real private Engram content ingested and no live production installation. The host-probe 0.9.27 candidate continues to be REVIEW-ONLY and MUST NOT be described as a usable memory installation. The new JS page is wired but is NOT yet served live by KiCom, and the host must enforce genuine first-party session, HTTPS, CSP (script-src 'self'), no-store, registration and appropriate rollback.

## Next concrete continuation, without user as terminal relay

Build the actual isolated/disabled-by-default KiCom first-party route in a COMPLETE protected candidate using existing original R3 source anchors and native verifier; register browser GET only for same-owner rendered drafts and authenticated POST only to the two JSON operations. Supply true server-side first-party browser session/verified owner mapping, non-webroot host configuration, review-approved operator consent, and no real personal data in public source or diagnostic logs. Design staging/host same-UID protection, release health/rollback and private encrypted storage policy. Once an exact reviewed package and safe host assumptions are established, seek Christoph's explicit authorization before the REAL installation and conduct a synthetic live-host write/read via a genuinely independent connected ChatGPT conversation. No paid action authorized. Do not circle back to generic BOOTSTRAP or iOS automation setup.
