# KiCom Engram — DEV-24: durable one-time exact-record memory consent ledger

Date 2026-09-20. Christoph wants a real persistent Engram across independent ChatGPT conversations. iOS Shortcuts activation and Slack already work in demonstrated test runs; do not ask Christoph to recreate the automations, repeat synthetic Mac/iPhone/external backup steps, or act as a technical relay. This checkpoint and public GitHub mirror contain TECHNICAL SOURCE + SYNTHETIC TEST RESULTS ONLY, never actual personal memories, private passkeys, session tokens, passwords, keys or backups.

## NEW executable progress, not a task-only handoff

Created `development/0.9.27/engram/KiComEngramPrivateConsentLedger.php`, an independent DEV-only private SQLite approval ledger for the existing `KiComEngramIngestionGate`. It accepts the seven EXACT fields the ingestion gate binds (verified subject, namespace, kind, body SHA-256, source kind, source-ref SHA-256, ordinary sensitivity). A private, server-side `independentReview(binding)` callback MUST affirm that a genuine human explicitly reviewed and approved that exact record before `issue` creates a random, short-lived receipt. This callback is a trust boundary: it is NOT YET IMPLEMENTED against live KiCom human approval, and caller-provided text, a fake `approved` flag, Slack/GitHub instructions or an ordinary DEV token alone are NOT valid approval.

The ledger uses private non-webroot 0700 storage, SQLite WAL and IMMEDIATE transactions, 0600 database/sidecar checks, UNIQUE outstanding exact-binding approval, and atomic `consume(binding)` for the memory adapter callback. No raw memory content is stored in this ledger; only hashes and receipt metadata. A consumed or expired approval cannot be reused; using the same record again needs a fresh, independently reviewed approval. If the memory write subsequently fails after consent consumption, the approval must be issued anew through a legitimate user action; this is a deliberate fail-closed semantics, NOT an exactly-once storage guarantee.

Created `development/0.9.27/engram/test-engram-private-consent-ledger.php` and included it in the existing full Engram synthetic CI suite. Real executable PHP regression, not a prose example: denied write without review, exact subject/body/namespace/provenance binding, single-use and cross-instance consumption, SQLite memory write via DEV-22 memory adapter only with an independently approved receipt, retrieval after a separately issued synthetic DEV session, other-subject denial, duplicate ingestion refusal and insecure directory/symlink/webroot denial. A first CI failure from the test fixture's PHP arrow closure capturing a stale identity map was fixed by capturing the mutable fixture explicitly by reference; first failed run 35524626610 MUST NOT be called passing. The corrected test run 35524672145 succeeded with **25/25** new consent-ledger assertions.

Updated `make-review-package.sh` to include the exact consent implementation and its regression in the NON-INSTALLABLE public code-only source review archive. Last tested CODE SHA **`035dcef2a9e36179944d9908cc6a4503a109e337`**. GitHub Actions `KiCom private Engram DEV` run **35524732178**, job **106114805607**, result **SUCCESS**: logs show `KICOM_ENGRAM_PRIVATE_CONSENT_LEDGER_TESTS_PASSED=25`, the two new files in the fixed-list review ZIP and `ENGRAM_STAGING_REVIEW_VERIFIER_OK=1`, `ENGRAM_STAGING_REVIEW_INSTALL_ATTEMPTED=0`. This is a synthetic offline regression, not a live host smoke test.
Evidence: https://github.com/cschymura/kicom-update-mirror/actions/runs/35524732178

## Critical live boundary / next development step

This module is still an isolated DEVELOPMENT implementation. No production KiCom memory route, real approved private Engram record or authorized ChatGPT cross-conversation read has been installed or demonstrated. The live user-verified consent issuer, real private host paths/vhost and same-UID isolation, config binding, identity-/rights registry provisioning, recovery/retention/deletion policy and complete rollback-capable KiCom package still need integration/review. The existing 0.9.27 REVIEW-ONLY host-probe ZIP DOES NOT contain a deployable memory feature; do not install it as if it does.

NEXT concrete implementation: create a single integrated synthetic end-to-end test wiring the existing original KiCom DEV session manager -> server-only credential reader -> private owner registry -> private one-time consent ledger -> memory HTTP-like adapter -> private SQLite store -> fresh separately issued session read. Then design server-side reviewed approval issuance based on actual first-party authenticated UI, with explicit permission and record binding; do not infer it from the existing generic DEV session. Package as one fail-closed, disabled-by-default, native-verifier-reviewed KiCom candidate with private config outside public webroot and rollback. Before REAL installation, verify host isolation and obtain Christoph's explicit bounded approval for the exact reviewed package. Do not paste actual memory bodies, keys or session data in Slack/GitHub.
