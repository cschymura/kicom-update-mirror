# Mirage Engram DEV-55 — native 0.9.32 OAuth/MCP integration candidate; installed 0.9.31 unchanged

2026-09-21. User confirmed 0.9.31 on actual All-inkl KiCom, healthy; real 0.9.29 synthetic SQLite and original KiCom signed owner enrollment previously completed. No production install, private-memory activation, new passkey or personal content in this DEV-55 turn. Prior checkpoint DEV-54: `543fa6ada214a115b8ed5f722f358a73a54dc032`.

## Actually developed + GitHub code

- `KiComEngramOAuthAuthorizeHttp.php` and `mirage-oauth-client.js`: a first-party authenticated **original KiCom admin** OAuth consent page and JSON challenge/confirm handler with explicit read consent, session-bound CSRF, original KiComPasskeyBridge real P-256 WebAuthn verification and original current private owner registry. The granted code can be issued exactly once, then exchanged via DEV-53 PKCE token endpoint; exact operator-pinned callback only.
- Extended `test-engram-oauth-signed-passkey.php`; CI workflow `.github/workflows/test-0927-engram-oauth-authorize.yml`. Final source-level original signed first-party WebAuthn → OAuth redirect/code/token and denial tests at GitHub Actions **35590835626 SUCCESS**, tested SHA `d3396337d553a8dc4c41e70c201f16c45db3b145`; **31/31 synthetic P-256 integration assertions**. Do not mistake for a real user's Live passkey assertion or real ChatGPT plugin login.
- The first-party real-client login challenge revealed that a fresh ChatGPT authorization browser has no KiCom admin session. Fixed original-password-first login branch in the EXACT operator 0.9.31 router patch, preserving session regeneration, then separate real signed-Passkey read consent: `development/0.9.27/engram/DEV55_0931_OAUTH_NATIVE_ROUTER.patch`, git blob `2b39650273bb2978e642ae3eadc9a95780e9b57b`, commit `ad6415909f0c9245c1dcf26f637a53fc69af6e65`. It adds `api.php` OAuth protected-resource/issuer discovery and token route, replaces provisional static MCP token handler with DEV-54 verified OAuth-token host bridge, `admin.php` first-party authorize/admin login+Passkey endpoint and first-party `.well-known` Apache rewrites. **All new routes default OFF** unless an already present separately approved private host runtime explicitly authorizes them.
- Exact tested six SOURCE bytes were exported as CI artifact `mirage-dev55-exact-oauth-source`, run **35591201395 SUCCESS**, commit `298b3306b3619961408fdc78e2ca237ba3cfc2f4`, artifact ID `10635095585`, including SHA256 source manifest. These six sources are now bundled in the 0.9.32 candidate (with earlier unchanged DEV-41..54 modules inherited from 0.9.31).

## Complete native installable candidate, independent local original-verifier proof

- The full package was constructed from EXACT `KiCom-0.9.31-Mirage-MCP-Route-INACTIVE.zip` SHA256 `9ad279b4bd084b13b8eba9327851440721c78f2894d1664d017fb72be21591c3`, with original api.php SHA256 `8fc7605a1d895efbeeadb7646b9809c4ded4c05e399ad8fe6a3921177545e3f4`, original admin.php SHA256 `41396e60bee4d8e494d9efd199fd3e4a7f5ec654867554de5e261db779edef70`, original .htaccess SHA256 `52865657c6fd1c449af85658f41f95abeb720e6f51daa6692d345dbfb1b05976`. The user screenshot proves installed 0.9.31 version, but a remote install SHA was NOT cryptographically verified without privileged host file read.
- **Native full 0.9.32 candidate** (ONLY local ChatGPT conversation artifact): `/mnt/data/mirage-dev55/KiCom-0.9.32-Mirage-OAuth-MCP-PREPARED.zip`, SHA256 **`298336e74ba91606c924eb67aa6e107799efc94ff6d9524f8bd648ce912eec53`**, 262881 bytes, **69 files**. 5 new PHP OAuth modules and JS; original guardian/recovery/index/living/PasskeyBridge untouched; native genome/manifest regenerated for direct parent 0.9.31→0.9.32. No private config/real token/SQLite/personal memory bundled.
- **Original native KiCom 0.9.31 self-update verifier accepted EXACT 0.9.32 ZIP** in isolated parent install fixture: class RED / separate human admin confirmation required, parent/genome correct, original recovery kernel preserved. Local verification script reports **65 PASS**, includes PHP lint on all 50 source files, JS syntax and 3 purposely corrupted ZIPs rejected.
- Actual local TCP HTTP test served the **exact extracted final ZIP**, with synthetic HTTPS+Apache rewrite simulation (NOT real TLS/All-inkl/ChatGPT): **14/14 PASS**, including inactive OAuth/MCP 404 with no private DB creation, synthetic first-party .well-known discovery, initial fresh-browser original KiCom password form, valid original password login 303 to same OAuth authorization URL, and password alone NOT able to mint a memory/OAuth token. Local PHP has NO `pdo_sqlite`, so the same final packaged ZIP was NOT full positive OAuth→SQLite HTTP-tested here; identical component sources were individually full-stack CI tested in DEV-54 (26/26) and original signed browser OAuth in DEV-55 (31/31), both synthetic.
- Developer-generated reproducibility/source/test bundle only in current conversation attachment `/mnt/data/mirage-dev55/Mirage-DEV55-Quellen-und-Pruefung.zip`, SHA256 `416fecd27b0bc5ec9c5892079454271be1925cba01e9a7e4ba123a086d6c711f`, 12 files. Builder scripts and final 0.9.32 ZIP are NOT hosted in GitHub Releases or KiCom auto-update feed. GitHub has exact OAuth source/patch/CI; do NOT invent new sandbox download paths in later sessions if unavailable.

## Remaining prerequisites; no false victory

- **Do not install 0.9.32 on production without separate operator confirmation**. Installing the candidate does not itself activate the user’s private memory, which remains OFF. Before any personal-memory activation, prove real All-inkl cross-vhost/alias/sibling PHP UID/backup isolation and receive explicit operator approval. The existing 0700/0600 modes alone do not show inter-app UID separation.
- The synthetic tests use `https://chatgpt.com/connector/oauth/callback` as a **dummy placeholder**, not a verified live ChatGPT callback. The exact real `client_id` and `redirect_uri` must be obtained from the actual ChatGPT Work Plugin connection UI or an authenticated client registration; pin in operator-only private host config, NEVER ship in public package or claim synthetic path is real. ChatGPT's stable CIMD callback mode requires issuer response `iss` and validated client-id metadata (NOT implemented/advertised); prefer exact per-plugin pinned real callback with preconfigured client until implemented.
- The server-side KiCom OAuth consent still requires actual existing admin session/Passkey, private OAuth+activation databases and correct host/operator policy, no endpoint can be fully activated from a client request. A complete **real** remote ChatGPT OAuth sign-in, server HTTP network roundtrip, private-memory and independent ChatGPT-instance recall have NOT occurred.
- Preserve original existing 0.9.31 source, real data and backup. Do not rerun previous successful operator SQLite/passkey tests, do not manually shuttle secrets or code between instances. Next meaningful task: resolve actual ChatGPT plugin client/callback via connected authorized workflow, complete live-host/UID/backup review using authorized access, and perform one real operator-approved staging OAuth/MCP synthetic end-to-end on All-inkl before personal-memory activation and new independent ChatGPT recall. Do not issue another inert update as a substitute for real connector setup.
