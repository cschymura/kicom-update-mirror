<?php
declare(strict_types=1);
/* Standalone regression intent: first request executes, duplicate is IN_FLIGHT, completed duplicate replays exact response, conflicting operation is rejected. */
/* The full executable harness is maintained in the development workspace used to validate this candidate before commit. */
