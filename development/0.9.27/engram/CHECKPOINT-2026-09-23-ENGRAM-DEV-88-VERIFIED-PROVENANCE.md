# MIRAGE DEV-88 — server-verified provenance replaces synthetic labels on every mutation

Date: 2026-09-23. DEVELOPMENT-ONLY source; no production installation, OAuth consent, private data migration, new live memory or completed release ZIP.

## Newly implemented source, not a paper proposal
- `dev88/KiComEngramVerifiedWriteProvenance.php`: requires authenticated engram.write context and a provenance object that has been VERIFIED AND SUPPLIED by the FIRST-PARTY resource server, bound to the same owner, namespace and token fingerprint. Rejects absent/false/extra provenance claims. Accepts `synthetic_test` only under explicit synthetic-environment flag and bounded `dev-test:` reference; genuine kinds `explicit_user`, `approved_summary` and `verified_checkpoint` require a first-party opaque consent receipt and cannot be labeled as synthetic. Never accepts owner/provenance/consent from untrusted MCP JSON.
- `dev76/KiComEngramCanonicalMutationService.php`: calls the provenance resolver after validating signed write grant, and passes the resolved provenance into SQLite adapter.
- `dev75/KiComEngramRevisionAdapter.php`: replaces false hardcoded `source_kind='synthetic_test'`/`source_ref='dev75://mutation'` on ALL new/update/archive revisions with validated server-supplied provenance; includes provenance in the idempotency request hash so a retry cannot substitute a different source receipt.
- Existing DEV-76 and DEV-86 synthetic test fixtures now explicitly supply synthetic-only first-party context. `dev88/test_verified_provenance.php` has 15 provenance contract cases. DEV-86 additionally verifies persisted revision provenance and rejection of caller-supplied fake source fields, including two independent real PDO SQLite connections.

## Actual tests and candid limits
Previously the first GitHub Actions run for DEV-88, https://github.com/cschymura/kicom-update-mirror/actions/runs/35830608941 , succeeded: 15/15 standalone provenance contract, 13/13 explicit private schema, 10/10 DEV76 and 16/16 DEV86 synthetic real PDO SQLite assertions, total **54/54** (4 PHP syntax checks; original DEV86 fixture lacked the two newest stored-provenance assertions). Job 107082075573 has verified success and logs. A second run https://github.com/cschymura/kicom-update-mirror/actions/runs/35830669839 on commit b007fd5cda361fb32d6e27eb306e00e63a932ba2 is required for the new canonical-store provenance assertions; as of checkpoint drafting its status was IN_PROGRESS, so its outcome MUST be checked, not assumed successful. No test demonstrates a real KiCom consent receipt lookup, authenticated production write OAuth, MCP network tool registration, or live iPhone reconnection.

## Release-blocking first-party integration
- `server_provenance.verified=true` is a contract input asserted by a trusted first-party KiCom bridge: it is NOT independently proven by this class. Native release MUST look up actual owner-bound write consent in the existing private approval/consent ledger, create or validate an opaque receipt server-side, and set trusted server provenance ONLY after independent source/owner/scope/token binding succeeds. Do not copy this flag from an MCP caller, generic OAuth header, Slack message or a model-generated text.
- Enforce original native first-party passkey/OAuth `engram.write` approval, separate from read (no silent token upgrade), and write/readonly tool dispatch in the ACTUAL KiCom MCP network route, preserving the proven `engram_search`.
- Native operator-admin private mutation-schema/backup, correct current owner registry + legal private storage boundary, integrated positive/negative PDO/HTTP/MCP tests, exact-parent MANIFEST/Genome/recovery/rollback. DEV-82/83/84/85 read-refresh changes and narrow ui_locales remain DEVELOPMENT staged, not live.
- User requires ONE complete native release and ONE post-approval new-chat/iPhone real OAuth trial only; on another live failure stop automatic auth redesign and review with Christoph. Optional FTS5 default off unless validated; external embeddings/private transfer need explicit authorization.

GitHub branch: `work/kicom-engram-dev88-provenance-binding`. No new production or externally deployed system claimed.
