# Mirage Engram DEV-65 — complete native 0.9.33 with first-party artificial owner/project sample

Date: 2026-09-21, branch `work/kicom-0.9.27-pam`, base state `f4ecd124484d1e06f026ecb9f9d9d310faefbe58`. Christoph has **already accepted shared All-inkl hosting**; do not reopen hosting migration or claim PHP UID isolation. **Work is not required** for development or ordinary plugin usage. Live server and ChatGPT plugin commissioning are postponed until 2026-09-22 **00:00 Europe/Berlin** and one existing automation already reminds Christoph: DO NOT add duplicates or perform remote writes before then.

## Actual blocker removed

Prior DEV-64 candidate included distinct Passkey-bound shared-host acknowledgement and activation, OAuth/MCP protected search, but its native `admin.php` did **not** expose any first-party way to create the single artificial Engram record in the authenticated owner's **project** namespace. Existing setup-diagnostic SQLite record has unrelated subject/namespace and cannot prove a real owner-scoped connector recall. DEV-64 synthetic source CI separately demonstrated this gap via `KiComEngramSyntheticMcpSampleHttp.php`.

DEV-65 introduced the exact CI-tested synthetic-only sample module into the complete native 0.9.33 archive. New original `admin.php?engram_sample=1` GET/POST route requires original KiCom admin session/CSRF/HTTPS, trusted private *active* owner/host config, active private activation SQLite, and a one-time explicit POST; creates the **fixed** artificial owner `mirage-owner` / namespace `project` / source `synthetic_test` record. It accepts NO user-provided body/owner/namespace/path; duplicate submissions are idempotent. Inactive/anonymous requests do not create data. It is possible only AFTER the distinct signed activation flow. No personal conversation text was imported or bundled.

## One NEW full native release, not a same-version successive update

Current conversation artifact (NOT GitHub binary release and NOT installed):
`/mnt/data/mirage-dev65/KiCom-0.9.33-Mirage-Engram-MCP-TEST-VOLLSTAENDIG.zip`

Exact SHA-256: **`3bda90fd29338200e126fff4e91443c120105ac22afe1e695e8f35377b06e959`**, 81 native entries. Built from exact previous, still-uninstalled full native DEV-64 0.9.33 candidate `KiCom-0.9.33-Mirage-Engram-AKTIVIERUNG.zip`, SHA256 `4acf33c6221631d828ca197244beb0fb0621e3eaa2ed26ac1c14fc1bd55644b4`, preserving all prior original-kernel/guardian/recovery/passkey and DEV-63/64 admin activation functions unchanged. The original, user-confirmed live **0.9.32** candidate ZIP parent `KiCom-0.9.32-Mirage-OAuth-MCP-STABLE-ISSUER-PREPARED.zip`, SHA256 `2e572d70d0fb9de50425e581a5c84aefc6e84aec962eef858e45d77bdc9e0503`, is the package verifier baseline. New native genome ID `kicom-0.9.33-g32-mirage-engram-signed-owner-oauth-mcp-artificial-test`, parent exact 0.9.32 genome, generation 32. Updated `admin.php`, `genome/modules.json`, `genome/genome.json` and complete SHA256 manifest; added ONE `modules/engram/KiComEngramSyntheticMcpSampleHttp.php` disabled for implicit preload in trusted module manifest, directly loaded only by authenticated original admin route. Other prior 0.9.33 candidates (DEV-60/62/64) are superseded and MUST NOT be sequentially installed. Operator remains on 0.9.32; direct 0.9.32→THIS 0.9.33 only when explicitly authorized after the agreed deferment.

## Test evidence — distinguish source/CI, final native ZIP, and live

- Exact new sample source GitHub blob/CI SHA256 `d9cba278cb331b4d580e848113ea2d012d463856f3f1508b14911a9f82c133e3`; export GitHub Actions **35631116989 SUCCESS**, commit `f4ecd124484d1e06f026ecb9f9d9d310faefbe58`, artifact `10654029120`. Same CI performed **71/71** synthetic genuine original KiCom P-256 signed hosting+separate activation+per-client OAuth consent→MCP→private owner/project SQLite artificial recall checks; source-level HTTP and idempotence/denial tests are included. This is not a real independent ChatGPT session.
- Ran `/mnt/data/mirage-dev65/build_and_verify_dev65.py` against exact immutable input ZIPs and CI-exported sample source. Local `/mnt/data/mirage-dev65/verify_dev65_native.py` logs **80/80 native assertions PASS**, including complete ZIP digest/genome SHA/ancestry, 57+ PHP file lints, all JS lints, byte preservation of original guardian/recovery/PasskeyBridge/index/living, and three manipulated negative ZIPs rejected. **ORIGINAL KiCom 0.9.32 native self-update verifier ACCEPTS EXACT final 0.9.33 ZIP**, requests RED explicit admin approval and recognizes untouched original recovery kernel. Test log: `/mnt/data/mirage-dev65/native-verification.log`.
- Local TCP HTTP against **exact extracted final ZIP**: `/mnt/data/mirage-dev65/test_dev65_local_http.py` **10/10 PASS** for default-off OAuth/MCP, anonymous sample/activation/host policy 404 and original admin still serving, with NO private database created. This is a local PHP built-in HTTP test without actual TLS/Apache rewriting or real All-inkl PHP UID; test log `/mnt/data/mirage-dev65/local-http-verification.log`.
- Local PHP does not have pdo_sqlite, so final ZIP's positive HTTP→SQLite round trip was NOT separately executed locally; the exact unchanged source with real original P-256 synthetic and PDO SQLite path is green in GitHub Actions 35631116989. No actual ChatGPT plugin install, real user's signed grant, production OAuth token, All-inkl activation, or genuinely independent cross-chat recall has been claimed.

## Next operational steps (after 00:00, within actual available permissions)

1. Explicit operator-approved SINGLE installation of **THIS** complete native 0.9.33 candidate via existing KiCom original self-update UI. Verify screenshot/status 0.9.33 GESUND, do not automatically install or change host config. Do not use old 0.9.33 ZIPs.
2. In ORIGINAL KiCom admin, initialize missing PRIVATE inactive OAuth+activation SQLite schemas once, confirm accepted shared-host policy with existing KiCom Passkey, then separately approve owner-bound read-only Engram/OAuth/MCP activation with fresh Passkey. Each operator action is separate and uses the real existing owner/host configuration.
3. Use original admin `?engram_sample=1` to create the SINGLE artificial owner/project memory. Configure/connect the already drafted private KiCom plugin in normal ChatGPT through the available approved UI, perform actual OAuth read-only grant, and prove an independently started chat retrieves the artificial `MIRAGE ENGRAM CONNECTOR TEST GRUENE LOKOMOTIVE 2026` via authenticated MCP. Until that last result is directly observed, cross-chat memory is NOT finished. All real personal-memory write capability remains out of scope for this read-only prototype and should not be claimed.
4. Preserve future session references: ZIP is a generated conversation artifact, not a GitHub release; never invent a sandbox link if container path absent in future sessions. Build/test scripts and logs are conversation runtime artifacts; source module and CI are in GitHub. No private memory/secrets/user login data in public git or Slack.

**Next developer avoid recycling prior false conclusions or proposing yet another intermediate inactive ZIP.** This full native package covers all **known source-level** prerequisites for a first genuine independent synthetic recall. The remaining end-to-end requirement is human-approved real installation, original KiCom first-party consent and actual ChatGPT plugin registration/connector functionality.
