# KiCom Engram — DEV-26: executable first-party single-record review form, test-proven in synthetic DEV

Date: 2026-09-20. Primary goal remains a usable PRIVATE Engram across separate ChatGPT conversations. The existing iOS automation trigger and Slack are already working in demonstrated trials. Do not request 48 automations be recreated, Mac terminal test repetitions, or manual transport of code/secret data. Public GitHub and Slack are technical DEVELOPMENT memory only, not a personal Engram store.

## New code and exact test

NEW `development/0.9.27/engram/KiComEngramFirstPartyReview.php` implements an isolated DEV-only first-party server-rendered HTML screen for reviewing ONE pending synthetic memory. `stage(trustedActor,entry)` checks server-supplied verified actor, namespace/write permission and strict memory fields, and places one draft in private 0700 SQLite storage for up to five minutes. `render(id,serverBrowserAuth)` requires an independently verified first-party same-owner browser context, returns escaped literal record content and a form with a randomly generated server-stored CSRF token; the form never contains a client-editable replacement record body. `confirm(post,serverBrowserAuth)` checks exact POST fields, rendered draft, current same-owner first-party context and **fresh passkey assertion reported by a trusted independently authenticated first-party callback**, before asking the existing private `KiComEngramPrivateConsentLedger` to issue its exact-binding single-use consent. It invalidates that draft for further approvals and returns a non-content confirmation. SQLite secure_delete and opportunistic expired/approved draft cleanup added, but do NOT claim secure erasure of past WAL/backup bytes or encryption at rest. No public route or real host configuration is added.

Critical trust boundary: this class does NOT perform fresh WebAuthn assertion verification itself; `verifiedBrowserReviewer` and the consent-ledger's independent-review callback MUST be implemented by a future actual protected KiCom first-party login/step-up controller, not by a caller-provided flag or standard DEV bearer. A client can submit the form, but a POST alone must NEVER grant approval. Host integration must enforce HTTPS, correct Origin/Referer, no-store response headers and first-party browser session fixation/revocation protections. The synthetic tests use a simulated verified first-party reviewer and synthetic independent-review callback, NOT a real person. Staged text is temporarily unencrypted in private SQLite and should NOT contain real personal memories until approved production retention/encryption policy is implemented.

NEW `test-engram-first-party-review.php` is wired into existing `test-engram.php`; exercises 19 synthetic positive/negative checks, including HTML escaping against script markup, absence of client-editable memory content, CSRF rejection, foreign owner and unauthenticated denial, ordinary login without fresh step-up denial, fake approval flag denial, exact review denial without independent review, single-use form, ledger-backed ingestion and independently issued new session retrieval of exactly the reviewed record.

Test truth: after a provisional privacy change the old test still expected `binding` in the return, so GitHub Actions run **35525519423 FAILED** on SHA cdfa109c046c7f0940605f81748350438f89f745. This was a test-fixture expectation mismatch; update to expect a minimal response, then re-ran full suite successfully.

Last tested CODE SHA **`17d319d1b51befe91a609f01b236e939254134f1`**; CI `KiCom private Engram DEV` run **35525574508** job **106117016625** SUCCESS with `KICOM_ENGRAM_FIRST_PARTY_REVIEW_TESTS_PASSED=19`, both source and test present in fixed-list NON-INSTALLABLE code-only review ZIP, original KiCom R3 native review `ENGRAM_STAGING_REVIEW_VERIFIER_OK=1` and `ENGRAM_STAGING_REVIEW_INSTALL_ATTEMPTED=0`. Link: https://github.com/cschymura/kicom-update-mirror/actions/runs/35525574508

## Actual present state and next specific work

Functional synthetic DEV core has: original KiCom DEV session authentication -> protected session-record passkey credential reader -> operator-provisioned private owner registry -> first-party single-record review form (synthetic identity/step-up callbacks) -> private one-use consent ledger -> authenticated memory adapter -> private SQLite -> fresh independent KiCom DEV session read. This is meaningful working, tested CODE, but it is NOT running on Christoph's real server or connected to a real independently opened ChatGPT conversation.

NEXT BUILD: implement a trusted KiCom first-party browser controller which (1) binds the reviewed draft to a genuine newly verified WebAuthn assertion for the same owner and the original text digest, (2) supplies the independent consent-ledger callback only for this exact verified user action, (3) routes protected POST/GET without exposing raw memory through GET bridge/Slack/GitHub, and (4) wires only fixed private host config. Test the COMPLETE candidate with native verifier and rollback, keeping live memory DISABLED until a scoped, explicit installation authorization and real-host isolation audit. Prepare one complete package, not another generic BOOTSTRAP loop. Do not claim personal-memory persistence in ChatGPT until a verified REAL connector transfers a synthetic online record between two independent ChatGPT instances.
