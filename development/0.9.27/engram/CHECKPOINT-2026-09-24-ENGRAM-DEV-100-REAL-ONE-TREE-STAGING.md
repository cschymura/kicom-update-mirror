# MIRAGE DEV-100 — ERSTMALS EIN ECHTER, VOLLSTÄNDIG GEMERGTER NATIVER ORIGINAL-QUELLBAUM

Stand 2026-09-23 / 2026-09-24 UTC. **STAGED ONLY: KEIN installierbares Gesamtpaket, KEIN produktiver Schreibzugang, KEIN Live-OAuth-Freigabeereignis, keine produktiven Datenbankänderungen.**

## Tatsächlich ausgeführter Original-Quellbaum-Merge — entscheidender neuer Meilenstein

Die tatsächliche Originaldatei `/mnt/data/KiCom-0.9.37-Mirage-MCP-META-FIX-VOLLSTAENDIG.zip` war in diesem Gesprächscontainer vorhanden und hatte die verifizierte SHA256 `0c6e02c64d44d603cb229f562d189f1b798bd78c2188fd2907e6b5cbafdb5ea7`. Das alte 0.9.37 enthält 82 Dateien. Die gesamte Development-Quellbasis auf dem Branch `work/kicom-engram-dev100-one-tree-validation` wurde zum ersten Mal mittels **GitHub-Actions-Quellartefakt** (nur die bestehenden öffentlichen DEV-Dateien; Original-ZIP, Passkeys, produktive Datenbanken, Tokens und Erinnerungen sind NICHT darin) tatsächlich und unabhängig frisch in den Arbeitscontainer gebracht. Export-GitHub-Actions run **35926317726** / Job **107402155595**, SUCCESS, Workflow `.github/workflows/kicom-dev100-source-bundle.yml`, Source-Commit **046b37f41294e8a0382463641ab66de0aabb0a81**, Artifact **10779895032**. Die ebenfalls überprüfte Source-Only-CI validierte vollständige Eingabequellen; Original-ZIP lag weiterhin NUR lokal im Gesprächscontainer, nicht auf GitHub.

Aus dem SHA-verifizierten Original und dem gerade AUS GITHUB neu geladenen Branch wurden über `dev97/stage_native_one_tree.py` **ALLE 11 Original-nativen DEV90–DEV99-Patches tatsächlich nacheinander mit GNU `patch --fuzz=0` erfolgreich angewendet**; die 13 zusätzlichen DEV-Module und das neue Browser-Asset wurden auf die normalen nativen Pfade flach gemappt. **74/74 PHP-Dateien bestanden `php -l`**, und alle 5 repräsentativen ursprünglichen/neuen PHP-Klassen wurden mit `require_once` gemeinsam geladen. Das ist jetzt eine echte vollständige lokale Original-Quellbaum-Zusammenführung, nicht bloß 11 formal gültige Diff-Dateien.

Dafür wurden drei zuvor produktionsblockierende historische Patch-Dateien auf dem DEV100-Branch als tatsächlich an Original-0.9.37 getestete Alternativen repariert:
- `dev100/NATIVE-exact-original-mcp-optional-write-protocol.patch`: der vorherige DEV90-Hunk endete ohne finale Newline, GNU patch verweigerte Hunk 3; die exakte abschließende Newline machte das vollständige 3-Hunk-MCP-Protokoll auf dem Original anwendbar.
- `dev100/NATIVE-exact-original-oauth-http-refresh-discovery.patch`: der alte DEV94-Entfernungs-Hunk #5 wurde wegen zu wenig eindeutigem Kontext auf dem echten bereits DEV93-gepatchten OAuthHttp zurückgewiesen. Der kanonische Diff wurde aus genau den bestätigten Alt-/Neuquelltexten neu erzeugt; alle OAuthHttp-Änderungen einschließlich token refresh und discovery sind daraufhin `--fuzz=0` anwendbar.
- `dev100/NATIVE-exact-original-api-first-party-write-factory.patch`: der alte DEV92-Patch schlug auf ursprünglichem api.php fehl; der neu erzeugte Diff gegen die tatsächliche Originaldatei ist `--fuzz=0` anwendbar und PHP-syntaktisch gültig.
- Der alte DEV93 Passkey-OAuth- und der DEV94 OAuthTransactions-Diff wurden auf diesem Branch um ihre fehlende finale Newline ergänzt. DEV99s bereits korrigierter DEV93-Austausch-Hunk `@@ -256,10 +345,17 @@` blieb erhalten. Stager wählt ausschließlich neue DEV100-Ersatzdateien, wenn ein vorheriger Patch nicht exakt anwendbar war.

Die **zweite, unabhängige Staging-Wiederholung aus dem frisch vom GitHub-Artefakt geladenen aktuellen Repo** ergab: `STAGED_ONLY`, 11/11 `applied_zero_fuzz`, 13 zusätzliche native PHP-Module, **74/74 PHP-Lints**. Dateivergleich mit verifiziertem Original: **96 Dateien total**, **82 alte Dateien erhalten**, **8 ursprünglich vorhandene Quelldateien gezielt modifiziert**, **14 neue Dateien hinzugefügt**, **keine entfernt**, und die originalen `guardian.php`, `recovery.php` sowie `modules/dev/PasskeyBridge.php` blieben **byteidentisch**.

## Tatsächlich ausgeführte zusätzliche Tests auf dem echten gemergten Original

- `dev100/test_full_staged_native_protocol.php` wurde mit genau diesem vollständigen nativen Original-PHP-Protokoll lokal ausgeführt. **6/6 synthetische PDO-Testdouble-Assertions PASS**: ohne serverinternen Writer nur `engram_search`; mit explizit serverinjiziertem Writer genau die vier gewünschten MCP-Operatoren; READ darf keine Schreiboperation ausführen; synthetische Schreiboperation erreicht ausschließlich den serverinternen Callback; MCP-JSON darf keinen Owner überschreiben; im inaktiven Original-Host werden keinerlei Operatoren offengelegt. Der Test verwendet eine Fake-Activation-PDO-Testklasse, er schreibt KEINE echte Erinnerung und prüft NICHT OAuth-/WebAuthn-Netzwerk/E2E.
- `dev100/prepare_nonproduction_genome.py` erstellt für den noch NICHT installierbaren Staging-Baum eine eigene Nachfolgeversion `0.9.38-g37-mirage-mcp-write-staged`, aktualisiert 95 SHA-Manifestzeilen und 88 Genome-Komponenten und registriert 65 Module. Neu hinzugefügte Engram-Module bleiben in der ursprünglichen modules.json **deaktiviert**, bis der erste KiCom-Betreiber-/Passkey-Pfad die neue Funktion wirklich freigibt. Mit diesem Staging-Metadatenstand meldete der bestehende `dev96/verify_native_release.py` nachweislich **`ok:true`, `mode:candidate-preflight`, `candidate_ready:false`**. Ein früherer Check VOR dem korrekten Metadata-Merge meldete erwartungsgemäß `MANIFEST_COVERAGE_MISMATCH`, das ist keine übergangene Prüfung.
- Für den lokal hier vorhandenen PHP-CLI-Treiber gilt: PHP 8.4 bietet `PDO` und `FFI`, aber **KEIN ext-pdo_sqlite**. Deshalb können native PDO-SQLite/HTTP/Passkey-Integrationstests aus diesem Container noch nicht als erfolgreich behauptet werden. Vorige synthetische echte PDO-SQLite-DEV75–95-Tests auf GitHub CI ersetzen NICHT den vollen jetzt gemergten Origin-Code E2E.

## Release-Blocker und unmittelbar nächste Arbeit

1. NEUE KONKRETE REALITÄT: ein EINZIGER vollständig zusammengeführter nativer 0.9.37-Nachfolgequellbaum existiert nun als **nichtinstallierbares Nonproduction-Staging**, ist aber noch nicht eine lauffähig freigegebene 0.9.38-Produktion. Kein neues isoliertes DEV-Modul statt Gesamtintegration erzeugen.
2. Vollständigen Original-Admin-/OAuth-/Write-Ready-/Passkey-Consent-Pfad einschl. korrekter aktueller Ownerrechte und privater REALER Active-OAuth-/Engram-SQLite-Sicherung und Migration gegen **echtes PHP ext-pdo_sqlite** isoliert testen. Im gemergten Runtime-Konstrukt muss die Originaladministration ausdrücklich die WRITE-Berechtigung setzen und widerrufen können; alte engram.read-Tokens nicht automatisch erweitern. Original api.php und First-Party Host müssen bei genehmigter Scope/Owner/Consent aktuellem Token alle vier Operatoren real ausführen; neue Schreib-Erinnerungen ohne falsche `synthetic_test`-Provenienz.
3. Original ersten/erneuerten/zweiten Chat OAuth / Refresh und aktuelle Owner-/Scope-/Host-/Token-Revoke- und Original-MCP-http-jsonrpc-Lese-/Schreibfälle mit echter PDO-SQLite, danach echte Passkey-WebAuthn Signaturen/browser/synthetischer Nonproduction-Hostingtest. Lokale sechs Fake-PDO-Assertions beweisen diese Dinge ausdrücklich nicht.
4. Original-KiCom-Updater, BACKUP/Wiederherstellung/Guardian/Recovery/Rollback und Datenschutz/Retention prüfen; erst DANACH EIN vollständiges Paket mit verifiziertem finalen MANIFEST/Genome und dem ursprünglichen Updater als *READY_FOR_RELEASE* anbieten. Der DEV96 Static-Gate-Erfolg allein darf kein Release-Fertigsignal sein.
5. KEINE produktive Installation oder Live-DB-/OAuth-/Rechteänderung ohne gesonderte konkrete Betreiberfreigabe. Danach GENAU EIN genehmigter neuer Chat-/iPhone OAuth-Live-Test; bei Fehlschlag automatische Auth-Umbauten STOPPEN und zusammen mit Christoph konkret KiCom- vs. ChatGPT-Plattform-Befunde untersuchen. Optional semantisches FTS5 nur überprüft und DEFAULT OFF; keine ungefragten externen privaten Embeddings.

Die vorliegende ursprüngliche ZIP wurde weder in GitHub veröffentlicht noch produktiv geändert; alle Tests verwendeten ausschließlich synthetische Konten/Daten.
