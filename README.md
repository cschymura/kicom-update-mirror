# KiCom Update Mirror

Public read-only mirror for verified KiCom release packages and the machine-readable update feed.

This repository is a transport mirror only. KiCom independently verifies package SHA-256, manifest, genome lineage, syntax, risk class, health checks and rollback requirements before installation.
