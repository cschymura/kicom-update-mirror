# Mirage Engram DEV-50 — vollständiges natives KiCom 0.9.31-MCP-Update gebaut und lokal geprüft

Datum 2026-09-21; Repository `cschymura/kicom-update-mirror`, Branch `work/kicom-0.9.27-pam`. Ausgangsstand: zuletzt dokumentierte Source-Export-Version DEV-49, Commit `eb168b8f35afd9eb88684857243174df733c5bd4`, mit acht exakten DEV-41–48 MCP-PHP-Dateien. GitHub Branch-HEAD und Slack-Claim DEV-48 vor weiterer Bearbeitung geprüft; es wurde keine produktive Installation durchgeführt.

## Konkretes Ergebnis, nicht nur ein Checkpoint

Ein vollständiges **natives KiCom-0.9.31-Update** aus dem exakten vorherigen 0.9.30-Originalpaket und dem DEV-49-geprüften Source-Export wurde lokal gebaut: `KiCom-0.9.31-Mirage-MCP-Route-INACTIVE.zip`; **SHA-256 `9ad279b4bd084b13b8eba9327851440721c78f2894d1664d017fb72be21591c3`**, 244117 Bytes, 63 eindeutige Archivdateien. Exakter 0.9.30-Parent: `KiCom-0.9.30-Mirage-Host-Audit-INACTIVE.zip`, SHA-256 `cb23764c9f49e2f1ed6afb06ff4baa4eba98cf4823c42c7a44c7f34d3cb48bb1`. Acht neue MCP-Module liegen im vollständigen Archive; `api.php` enthält die neue KiCom-First-Party-Route `q=ENGRAM_MCP` unter serverseitig geprüftem, STANDARDMÄSSIG DEAKTIVIERTEM Host-Loader. `lib.php` meldet 0.9.31, native Genome-Elternschaft 0.9.30→0.9.31 und SHA-MANIFEST aktualisiert. Recovery/Guardian/Index/Admin und sämtliche existierenden .htaccess-Dateien sind bytegleich zum geprüften Parent. Kein privates Credential, Bearer-Token oder personenbezogenes Engram im ZIP.

**Paketprüfung tatsächlich durchgeführt:** Lokales Prüferskript `verify_release.py` lieferte 63 PASS-Einzelprüfungen inklusive 45 PHP-Syntaxprüfungen. Der ORIGINALE KiCom-0.9.30-Self-Update-Prüfer, ausgeführt gegen eine isolierte unveränderte 0.9.30-Installation, akzeptierte das exakte neue 0.9.31-ZIP mit direkter Genome-Abstammung, unverändertem Recovery-Kernel und **Risikoklasse ROT / menschliche Adminfreigabe**. Drei unabhängig beschädigte Testpakete (manipuliertes MCP-Modul, fehlendes Modul, falscher Parent bei neu berechnetem Manifest) wies er zurück. Keine native Produktionsfreigabe oder Installation behaupten.

**HTTP-Netzwerktest tatsächlich durchgeführt:** `test_http_inactive.py` entpackte das exakte neue ZIP auf eine isolierte lokale PHP-Webserver-Instanz. Drei HTTP-Anfragen gegen `api.php?q=ENGRAM_MCP` mit inaktivem realistischem Host-Default (POST einschließlich Bearer-Platzhalter, vermeintliche Client-Aktivierungsheader, GET) lieferten alle **HTTP 404 mit 0 Antwortbytes**. Dies weist nur den inaktiven Fail-Closed-Zweig des VOLLSTÄNDIGEN Archivpakets nach. Positiver HTTP/MCP→SQLite-Test und 26/26 originaler First-Party-Bridge-Integrationstests wurden zuvor mit DEV-Fixtures in CI durchgeführt (DEV-47/48), nicht mit dem tatsächlichen All-inkl-Host; keine reale ChatGPT-MCP-Verbindung.

**Herkunft und Verfügbarkeit:** Das komplette 0.9.31-ZIP, `build_release.py`, `verify_release.py`, `test_http_inactive.py` sowie ein gebündeltes Quell-/Prüfarchiv `Mirage-DEV50-Quellen-und-Pruefung.zip` (SHA-256 `99cc38d3f9e06787e37057cba0fda3c71fef8415ddbb9c606fe9e2d07c7fdc94`) liegen als herunterladbare Artefakte im aktuellen Chat unter `/mnt/data/Mirage-Engram-0.9.31/`; **NICHT** in einem offiziellen GitHub-Binary-Release, NICHT im automatischen Updatefeed und NICHT auf dem All-inkl-Server installiert. Das source-export GitHub-Artefakt und die DEV-PHP-Dateien wurden unabhängig davor unter DEV-49 bereits archiviert; das neue lokale Binärarchiv ist nicht durch einen GitHub-Runner erstellt worden. Bei späterer Wiederaufnahme keine Bytes oder User-Zugänge aus dem Checkpoint erfinden: aktuelle Chat-Artefakte bzw. explizit vorhandene GitHub-CI-Quelle verwenden.

## Operatorstatus / der eine nächste echte Schritt

Christophs letzter im Chat bestätigter Serverstand ist KiCom **0.9.29** mit bestandenem synthetischem echten SQLite-Roundtrip und gespeicherter Passkey-Besitzerzuordnung. 0.9.30 wurde als inaktives Host-Audit erstellt; seine LIVE-Installation wurde **nicht bestätigt**. Die native 0.9.31 kann **NUR von 0.9.30** mit ROT-Freigabe installiert werden; falls Server noch 0.9.29 hat, muss zuvor das geprüfte 0.9.30-ZIP genehmigt installiert werden. Nicht von einem bloßen Updateartefakt auf den produktiven Versionsstand schließen. Alte SQLite-/Passkey-Tests NICHT wiederholen lassen.

Auf All-inkl sind weiter NICHT nachgewiesen: vollständige VHost-/Alias-/PHP-UID-/Backup-Isolation, gesonderte Serveraktivierungsfreigabe und echte Host-/Owner-/Connector-Verbindung. Die derzeitige First-Party-MCP-Route liefert absichtlich 404, selbst nach 0.9.31-Installation; reale private Erinnerungen bleiben INAKTIV. Der Stand DEV-47/48 besitzt einen privaten Bearer-Verifier in synthetischen Tests, aber noch keinen realen passkeygesicherten Token-Issuer/Revoker und keinen nachgewiesen kompatiblen echten ChatGPT-Plugin/OAuth-Connector. Unabhängiger ChatGPT-Abruf ist NICHT erfolgt.

Als nächstes: bei autorisiertem Host-Zugang aktuelle installierte Version lesen und 0.9.30/31 nur bei ausdrücklicher Operatorfreigabe installieren, realen Host-/Backup-Befund beschaffen und die Original-Owner-/Host-Freigabe samt passkeygeschützter Connector-Credential-Ausstellung und von ChatGPT unterstützter OAuth-Anmeldung bauen/testen. Den Betreiber nicht als technischen Boten einsetzen, solange direkte Werkzeuge verfügbar sind. Diese SOURCE-/BINÄR-Übergabe ist ein nachweisbarer **Installationskandidat**, KEIN erfolgreiches Live-Langzeitgedächtnis.
