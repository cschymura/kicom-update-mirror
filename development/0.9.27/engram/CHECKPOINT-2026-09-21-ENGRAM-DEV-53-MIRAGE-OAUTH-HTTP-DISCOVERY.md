# Mirage Engram DEV-53 — OAuth discovery, 401 challenge and strict HTTP PKCE token exchange, CI GREEN

Date 2026-09-21. Previous checkpoint DEV-52 commit `888209624285e56597b14195e871bf831662c697`; current development branch `work/kicom-0.9.27-pam`. Christoph's operator screenshot confirms real KiCom 0.9.31 installed and healthy. The original full 0.9.31 package was inspected; existing `api.php` hash SHA-256 `8fc7605a1d895efbeeadb7646b9809c4ded4c05e399ad8fe6a3921177545e3f4`. Do not use old public main 0.9.26 as live code.

## Actual source work

1. `KiComEngramOAuthHttp.php`: first-party, transport-neutral HTTPS OAuth HTTP facade with fixed KiCom resource `https://kicom.rurtalbahn.info/api.php?q=ENGRAM_MCP` and issuer `https://kicom.rurtalbahn.info`. Provides protected-resource metadata (`authorization_servers`, exact `resource`, `engram.read` scope), authorization-server metadata (`authorization_endpoint` `admin.php?engram_oauth=1`, `token_endpoint` `api.php?q=ENGRAM_OAUTH_TOKEN`, S256 PKCE, public-client `none` method, authorization-code grant), and OAuth 401 `WWW-Authenticate` challenge with resource-metadata URL. POST form-urlencoded token exchange requires exact HTTPS origin/host/method, strict no-duplicate six-field parser, pinned server-side client, single-use code/PKCE, same exact resource and host policy, with bounded no-store response and generic invalid_grant failures. Real installed 0.9.31 lacks `oauth_enabled` and remains 404 before metadata/token logic.
2. `test-engram-oauth-http.php` and `.github/workflows/test-0927-engram-oauth-http.yml`: check metadata fields/headers, inactive host, false operator/host isolation flags, 401 resource metadata challenge, host/HTTPS/method/Content-Type/Authorization denials, duplicate/injected/malformed form fields, foreign resource, valid exact PKCE exchange, pinned identity, one-use code and revocation. OAuth approval fixture is SYNTHETIC; actual signed WebAuthn approval is separately tested in DEV-52.
3. `KiComEngramMcpProtocol.php` now publishes tool-specific `securitySchemes=[{type:'oauth2',scopes:['engram.read']}]` on the only read-only `engram_search` tool. Updated `test-engram-mcp-protocol.php` and reran existing end-to-end synthetic MCP suite.

## Exact immutable verification

- OAuth metadata+HTTP code/test/workflow SHA **`5597f2fce70f78ca223b7610586c27b7d42c95f1`**. GitHub CI run **35588837047**, job **106298356490**, `success`: R3 immutable release/source check, 3 PHP syntax checks, **32/32 synthetic OAuth HTTP assertions**. https://github.com/cschymura/kicom-update-mirror/actions/runs/35588837047
- MCP `securitySchemes` code/test SHA **`35be85c8017cb5c65d2abe20da88c411159966f1`**. CI run **35588918322**, job **106298616774**, `success`: 7 PHP lint checks, **37/37 synthetic MCP assertions**. https://github.com/cschymura/kicom-update-mirror/actions/runs/35588918322
- DEV-52 final signed-Passkey/CSRF CI on commit `d5af869e854dc31868a430510340e553043b5a45`, run **35588532126** `success`: original KiCom P-256 verified signature -> owner-bound OAuth code tested synthetically (no live user involvement).

## Important nonclaims and next executable deliverable

**NO production installation/activation or real OAuth HTTP endpoint is present yet.** `KiComEngramOAuthHttp.php` is a CI-tested development class, not deployed or wired to 0.9.31; its discovery/authorization/token URLs are FUTURE routes, currently not guaranteed reachable. The current static DEV-47 bearer record and live 0.9.31 `q=ENGRAM_MCP` must NOT be described as OAuth-ready. `client_id_metadata_document_supported=false` is deliberate until a pinned real ChatGPT CIMD document can be retrieved+validated; no false DCR, issuer-ID callback, or refresh-token support claimed. Existing first-party signed Passkey grant requires a real authenticated HTTPS `admin.php?engram_oauth=1` UI/POST route with the exact original KiCom session/CSRF; authorize/code+redirect and `/.well-known` rewrites, token endpoint and replacing provisional static token verifier with DEV-51 OAuth token verification at the existing MCP resource remain unimplemented. No real All-inkl vhost/UID/backup isolation attestation or independent ChatGPT chat memory fetch.

Next: implement the COMPLETE 0.9.31-derived inactive-first OAuth router/consent page/token endpoint, integrate OAuth bearer verification into existing KiCom MCP bridge and test HTTP positive/negative using the exact installed base source, then build a single native full update (0.9.32) with original KiCom self-update verifier and rollback, only after a suitable operator authorization. Do not ask Christoph to repeat his successful real 0.9.29 SQLite or Passkey binding tests. Do not claim an inert metadata class alone completes independent cross-chat memory.
