# KiCom Engram — DEV-18 own-PC/external encrypted backup DEV + controlled HOST-PROBE REVIEW artifact

Date: 2026-09-20. Continue DEV-17, COLLABORATION-PROTOCOL.md and TASK-ENGRAM-PRIVATE-MEMORY.md. User expressly authorized PREPARATION of a controlled synthetic-only All-inkl host test via ordinary protected KiCom update route, but requested full scope/effects notification BEFORE actual installation. Independent copy is chosen for the USER'S OWN COMPUTER OR EXTERNAL DRIVE. NO paid service without further agreement. PUBLIC GitHub contains only code, synthetic tests and technical evidence — no personal engrams, real messages, host directory secrets, private keys, actual DB/backup bytes, OTPs, session IDs or private retrieval results.

## Canonical live basis and exact scope

Re-read live BOOTSTRAP and all canonical KiCom resources during this cycle; live version remains **0.9.26**. DEV-17 predecessor tested SHA \`5f420ff9f050ef5c3638b68be110313d780d362b\`, CI 35507403041 (335 synthetic tests). The original protected 0.9.26-R3 archive/source SHA preflight was repeated successfully. No KiCom production/DEV service, KAS setting, user WebFTP file, DNS, actual private Engram content, API role, actual passkey/FreeOTP/secret or protected update has been changed. No server/test-package upload or installation has occurred.

## New executable synthetic DEV implementation

- \`KiComEngramSyntheticTransfer.php\`: isolated CLI-only, synthetic-only fixture creates a fresh SQLite DB with TWO artificial memory revisions in a randomly allocated private scratch directory. Makes one consistent audited SQLite snapshot, bounds it to 2 MiB and encrypts it with libsodium sealed box to a supplied **public** Curve25519 key. Writes only private-mode 0600 JSON ciphertext/metadata in an operator-supplied non-web directory; removes only its own fresh synthetic plaintext scratch after encryption. No existing actual engram/database/backup argument or real memory source accepted. No private workstation key is ever transferred to the host. This is not an HTTP endpoint or actual host deploy artifact.
- \`engram_local_offline.py\`: standalone Python 3.11+ offline PC-side CLI with free, pinned PyNaCl==1.5.0. \`generate-key\` exclusively creates a local Curve25519 private key (0600 on POSIX, with OS/encrypted disk/ACL caveats on Windows) and emits only the public key/digest. \`inspect-synthetic\` requires a separately noted SHA-256 of the sealed bundle, validates strict synthetic format, decrypts solely on the PC and uses in-memory SQLite + full 2-revision content-hash/lineage audit. Returns bounded metadata only; refuses any nonsynthetic memory bodies/source_kind, wrong key, digest mismatch, tampered envelope and repeated key creation. No real personal DB is imported; SQLite plaintext is never written as a workstation file in the synthetic test.
- \`test-engram-offline-transfer.php\`: 16 complete cross-language synthetic tests using the *actual PHP sealed-box export and actual Python/PyNaCl local decryption* with an ephemeral workstation private key. No real key or backup was copied to GitHub. This confirms confidentiality/transfer mechanics but NOT sender authentication: anyone with public key can seal arbitrary payload. Separate trusted Ed25519 anchor/identity/anti-rollback and legitimate release controls remain mandatory for real backups.
- Updated dedicated Engram CI to install free PyNaCl==1.5.0 with Python 3.11 and PHP sodium, run Python/PHP lint and all existing negative-boundary tests.

## NON-INSTALLABLE operator review ZIP

- \`HOST-PROBE-UND-LOKALES-BACKUP-REVIEW.md\`: detailed German scope/effects, protected update/revalidation, real-host limitations, rollback/stop conditions, local-key workflow and reasons no personal data may yet be imported.
- \`make-review-package.sh\`: fixed allowlist only: code, synthetic fixtures, offline PC client, exact original-R3 DEV baseline for review, documentation and SHA256 file list. **No private keys, SQLite DB, server secrets or any live files**. Archive has NO deployable KiCom release/genome/install manifest. DO NOT upload it to KAS, WebFTP or the KiCom updater. It is source-review material; the exact protected release/diff must be built separately from actual verified live baseline after operator approval.
- The existing CI workflow BUILDS and uploads an artifact called \`Engram-Hostprobe-REVIEW-DEV\` (ZIP inside artifact). GitHub Actions run **35507938960**, job **106070790417**, all steps PASSED including separate code-only ZIP packaging and upload. Artifact ID **10604253158**; CI log SHA-256 of inner \`Engram-Hostprobe-REVIEW-DEV.zip\` = **\`804fc7839d6fc9d22e0aca0f23138f7e07eb35566d43d078ff5474d1b62443d3\`**. Artifact retention currently 14 days; the review package builder and all source persist in the repository for regeneration after expiry.

## Pinned executable CI

**Tested executable SHA \`28507e69ca2e32b1b6d71383c43acd0fcaf6e3f9\`**.
Run https://github.com/cschymura/kicom-update-mirror/actions/runs/35507938960, job **106070790417**, result **success**.
Existing complete suites: main 49 + path 23 + DEV route 23 + endpoint 14 + integrity 16 + ingestion 29 + mirror 46 + exception fault 45 + SIGKILL 44 + concurrency 8 + signed anchor 22 + catalog 16 + offline transfer 16 = **351 synthetic checks**.
Existing untouched original R3 SHA/package/source preflight passed; PHP lint, Python bytecompile and code-only package build/upload passed. README and this checkpoint are documentation: they do not themselves prove runtime behavior. 

## Explicit operator action before actual installation

1. Review the non-installable package and human-readable HOST-PROBE-UND-LOKALES-BACKUP-REVIEW.md, including exact proposed DEV module additions, 32-byte synthetic temporary files and cleanup. User's earlier PREPARATION approval is NOT an approval to install/activate this package.
2. First validate *complete* real KAS/All-inkl webroot inventory including provider/default-host aliases and the actual live KiCom source/module/Genome SHA, PHP effective UID and any \`open_basedir\`/same-UID cross-app isolation. Never hardcode real host paths into PUBLIC GitHub. If necessary obtain operator-controlled source/config by approved private channel. A successful 32-byte KiCom probe alone cannot establish another PHP application's inability to read the same private directory.
3. Build a SEPARATE signed/verified, human-approved, normal KiCom update release for a bounded DEV-only probe once the current installed baseline and trusted fixed PRIVATE non-web config are reviewed. Keep standard verifier, archive, healthcheck, backup/rollback, passkey, admin, production and protected-action boundaries unchanged. Require explicit human approval **immediately before** any live install. The existing private data/backups directory and harmless canary are already operator-provisioned; do not ask user to recreate them.
4. On user's own PC, the local workstation key may be generated ONLY when the user chooses their OS and protected local key directory; do not ask them to paste their private key into chat. Later transport of a synthetic encrypted bundle must use an authorized private path; the unimplemented real private-memory exporter must never be inferred from this CLI fixture. Encrypt/bound/bind real snapshots, independent offline signed anchor/counter, key recovery, complete retention/erasure including every external drive and all old immutable revisions, and real user-scoped consent before real memory import.
5. No subscription or second hosting account requested or purchased. No periodic background work or automatic server sync has been scheduled. Current public GitHub Actions artifact is not a private-data backup and does not constitute a production install.

At next chat resume from this checkpoint, verify live BOOTSTRAP/latest CI, continue only reviewed safe work and stop at exact protected live-install authorization boundary. Documentation-only checkpoint commit is NOT a new tested-code SHA.
