# Next milestone

Perform the first controlled external Expansion Cell deployment against an operator-authorized HTTPS test domain using a temporary FTP/FTPS account. Validate actual shared-host behavior for upload, `.htaccess`, first bootstrap, enrollment, activation, signed federation tick, and removal/deactivation of the temporary transfer account.

Do not use production application directories for the first test; deploy only into the dedicated `kicom/` directory beneath the supplied test web root.
