# Mirage Engram DEV-48 — first-party KiCom MCP bridge against original 0.9.29 owner registry

Stand 2026-09-21. Branch `work/kicom-0.9.27-pam`. Starting parent DEV-47 checkpoint `08c69c6eaeb6b5c95f3e5bb6b52099cefd07f8dd`. Conflict checks against GitHub and Slack completed before claiming 30-minute DEV-48 task. No other worker claim observed.

## Concrete durable source implementation

- Added `KiComEngramMcpFirstPartyBridge.php`, `test-engram-mcp-first-party-bridge.php`, dedicated `.github/workflows/test-0927-engram-mcp-first-party-bridge.yml`.
- Bridge does NOT merely accept a simulated preverified owner array: it loads the existing original KiCom `KiComEngramPrivateOwnerRegistry` from an operator-owned fixed private file, verifies the trusted web root and sibling private directories, and opens only a PREEXISTING 0600 private SQLite activation receipt. It composes DEV-47 private hashed bearer HTTP transport, DEV-46 MCP protocol and DEV-44 read-only runtime gate with the actual `KiComEngramStore`.
- The actual KiCom 0.9.29 `KiComEngramNextStep::bindEmptyRegistry()` was examined from byte-verified 0.9.30 parent ZIP. Its single-owner record has `enabled=true`, `credential_fingerprint`, `subject=mirage-owner`, `namespaces=['project']`, `engram_rights=['engram.read','engram.write']`. DEV-48 fixture mirrors those actual record FIELD NAMES and reads them through the original registry class, without repeating Christoph's already successful real passkey enrollment.
- Under 0.9.29/0.9.30 `setup-pending` config, with missing activation receipt/token, the bridge fails before adapter/store creation. No new auth secret, passkey, activation DB, host approval or SQLite file is auto-created from a request. For a synthetic active policy only, private signed-owner lookup plus token, activation, transport, tool call and SQLite roundtrip complete.

## Exact executable green proof

Code/test/workflow GitHub SHA **`4591db7f4b463580a52f4e96fb77c07a7730ebf7`**, GitHub Actions run **35580684650**, job **106272598564**, outcome **success**. Original immutable R3 ZIP + source manifest check passed; 11 PHP files lint clean. **26/26 executable synthetic integration and negative checks** passed, including first-party SQLite/MCP roundtrip, actual owner-registry schema, disabled 0.9.29-style scaffold, missing and unprotected activation DB, revoked/foreign passkey record, bad bearer, path injection and denied writes. Run: https://github.com/cschymura/kicom-update-mirror/actions/runs/35580684650

## Actual original api.php integration preparation (not native release yet)

The exact 0.9.30 source ZIP `KiCom-0.9.30-Mirage-Host-Audit-INACTIVE.zip` (SHA-256 `cb23764c9f49e2f1ed6afb06ff4baa4eba98cf4823c42c7a44c7f34d3cb48bb1`) was verified in the active local session; original `api.php` SHA-256 `bcf9370548ec7b56f6afb90531dfb7ceb7a69fad2ffc024951e04d2c7066e428`. The API already contains the `ENGRAM_MEMORY` and `ENGRAM_REVIEW` branches and `kicomEngramServerRuntime()` host-owned loader. A LOCAL source patch inserted a new `ENGRAM_MCP` branch before them with default false gates; PHP lint passed, duplicate/foreign-parent checks passed. An isolated PHP built-in HTTP server issued a real POST to this patched api.php with the parent default inactive host setup and obtained **HTTP 404, zero body bytes**, without a module load or DB write. This is a real localhost HTTP negative test, NOT a live All-inkl or connected ChatGPT test. This local patch has **NOT been packaged as a native 0.9.31 installable release**.
- A reproducible **same-purpose** source patcher is now stored at `development/0.9.27/engram/patch_0930_first_party_mcp_router.py`, commit `25fcfa9951f361cc4dce8cf1ebb9b01a8555e9cf`. It enforces the ORIGINAL 0.9.30 api.php hash and unique anchor; it is source preparation, NOT a deployable ZIP or evidence of full installed routing. The local proof above used its independently prepared local counterpart; do NOT falsely claim an exact-byte CI build of the stored patcher or of the full 0.9.31 native package.

## Actual blockers / next executor

- No confirmed 0.9.30 LIVE installation from Christoph yet: last confirmed real server version 0.9.29 with synthetic SQLite roundtrip and signed owner binding. Do not overwrite a live instance or claim route exists on All-inkl.
- A COMPLETE native follow-up release must include **all** DEV-41..48 MCP PHP files, original KiCom owner registry and a correctly patched api.php, correct genome/manifest/rollback, immutable parent and original self-update verifier + negative tests. Do not ship a route that requires modules omitted from the ZIP. The 0.9.30 source archive is only a CURRENT-CONVERSATION working artifact, not a GitHub release. A complete binary release or real host deployment has NOT been produced in DEV-48.
- The real All-inkl cross-vhost/PHP-UID/alias isolation and genuine backup restoration are not proven by 0700/0600; the full operator-owned host activation configuration and separately authorized credential/token issuance/revocation are still absent. The DEV-47 private bearer token scheme is NOT a demonstrated compatible ChatGPT OAuth connection, and the MCP POST handler is not independently tested against an actual registered ChatGPT plugin.
- Do not ask Christoph to repeat SQLite/passkey steps or claim independent chat recall. Next work should reconcile exact auth/consent and native packaging, perform a real synthetic-host network test of the full packaged source including positive/negative access, then obtain actual host evidence and operator authorization for production activation.
