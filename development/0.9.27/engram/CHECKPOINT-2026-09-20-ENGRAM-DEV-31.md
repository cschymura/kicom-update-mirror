# KiCom Engram DEV-31 — package with original-admin first-party signed review AND automatic private host-config discovery

Date: 2026-09-20. Christoph's objective: genuinely persistent, private Engram shared by freshly activated independent ChatGPT workers without user as terminal/messenger. His existing iOS scheduling and Slack work. No repeated BOOTSTRAP probes. This public handoff contains NO personal memories, credentials, passkey data, private host paths or user identifiers.

## Actual, completed executable work
- New `KiComEngramFirstPartyHostBridge.php` binds each reviewed draft to the ORIGINAL password-authenticated KiCom admin PHP session + existing CSRF token, fixed operator-configured owner and original KiCom WebAuthn verifier's FRESH P-256 signed same-owner passkey step-up; a mere DEV bearer, cross-user session, CSRF value or model content cannot approve.
- Native allowlisted `admin.php?engram_review=1` exposes a private server-rendered stage/review page ONLY after actual original admin login + a separately approved server-owned runtime. Native `api.php?q=ENGRAM_REVIEW` provides same-origin/HTTPS JSON BEGIN/CONFIRM bound to that first-party admin session. Updated browser JS preserves query string and calls navigator.credentials.get; `api.php?q=ENGRAM_MEMORY` consumes the exact one-time consent before authenticated scoped private SQLite write or read.
- CRITICAL DEPLOYMENT IMPROVEMENT: existing allowlisted `lib.php` has a fail-closed `kicomEngramServerRuntime()` loader for fixed `engram-private/engram-host.json` in the KiCom webroot's sibling outside the document root, requiring private 0700 directory/0600 single-link fixed-name JSON, schema1, exact webroot binding and separately attested server-only host flags. NO manual PHP auto_prepend_file is needed. The release includes no host config or secrets and remains 404/disabled by default.
- Existing tests now cover original authenticated KiCom admin session -> exact record review -> actual signed synthetic P-256 passkey assertion -> one-use private consent -> native KiCom memory write -> independent new KiCom DEV session recall. `KICOM_ENGRAM_REAL_WEBAUTHN_APPROVAL_TESTS_PASSED=34`.
- Real localhost HTTP acceptance traverses the EXTRACTED RELEASE ZIP's api.php both without private host config (404/no DB) and with an independent ephemeral 0600 synthetic sibling config read by installed lib.php, and proves exact one-time consent write, distinct session recall and cross-owner/anonymous denial: `ENGRAM_NATIVE_HTTP_SYNTHETIC_ACCEPTANCE_PASSED=7`; `ENGRAM_NATIVE_HTTP_PRIVATE_HOST_BOOTSTRAP_AUTODISCOVERED=1`.
- Full existing Engram CI and original native KiCom R3 red-risk self-update verifier succeeded on code SHA **3fc8aa389739ef42575719c4ac8444df32f450aa**. GitHub Actions run **35534000724**, job **106139639990**, success. Exact release ZIP `KiCom-0.9.27-Engram-INTEGRATED-DISABLED.zip`, SHA-256 **38e9fce1a2d3f31fda45b04bfd29c13b38a05d0fb7033b1f9916a68461185d26**, 215664 bytes. Actions artifact ID **10611249893**, enclosing bundle `KiCom-0927-Engram-INTEGRATED-DISABLED-REVIEW`; release archive and build-record JSON inside. Native verifier accepted, baseline kernel unchanged, 16 memory modules carried but globally disabled, human approval required, NOT installed/published to update feed. https://github.com/cschymura/kicom-update-mirror/actions/runs/35534000724

## Truthful current state and the actual next action
- Read-only Opera connector confirms current KiCom admin still reported **0.9.26**; this 0.9.27 package was NOT installed. The assistant can read the All-inkl WebFTP UI via Opera but presently has NO authorized browser write/upload/FTP action in that connector, so cannot claim deployment. Publishing to a GitHub feed != installing on All-inkl.
- Do NOT mark personal Engram live. An operator must verify actual ALL vhosts/aliases and same-PHP-UID/private-host config; the 0600 JSON operator authorization/owner registration, private storage protection/backup/retention and real passkey enrollment are NOT provisioned. Tests use synthetic credential+consent only. A distinct REAL ChatGPT conversation still lacks a connected authenticated KiCom private-memory connector/service credential; a new PHP/DEV session is NOT equivalent.
- Priority after explicit scoped approval: put exact RED-risk package through existing KiCom self-update control plane with native backup/health/rollback, examine real host through first-party protected interface, provision reviewed external config/owner registry (never via GitHub/Slack), then separate first-party real signed consent and synthetic server write; implement authorized ChatGPT connector read/write and verify an ACTUAL newly activated worker recalls the same record. Existing iOS automations untouched.
- Do not ask Christoph to run repetitive commands or relay worker results. If no authenticated server write connector is available, state the precise tool boundary once and present one bounded approval/connection step rather than reporting generic failure. Do not install/enable real-memory ingestion based on public synthetic CI alone.
