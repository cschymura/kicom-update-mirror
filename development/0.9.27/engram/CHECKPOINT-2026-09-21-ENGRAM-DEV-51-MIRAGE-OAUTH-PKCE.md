# Mirage Engram DEV-51 — OAuth 2.1 PKCE core (not yet a connected plugin)

Date: 2026-09-21; branch `work/kicom-0.9.27-pam`; starting checkpoint DEV-50 `df414130b186d85d0500d7d804661bb3b0d5c449`. Christoph uploaded a KiCom admin screenshot confirming real installation 0.9.31, health GESUND (0.9.30→0.9.31). Real synthetic SQLite and original first-party Passkey-owner binding had previously been confirmed in 0.9.29; no repeat user tests are needed. Opera Browser Connector was queried once this turn and returned "Browser not connected"; no direct All-inkl admin write permission. The existing host config still has no independently verified vhost/alias/PHP UID/backup evidence; do not claim real private Engram activation.

## Critical compatibility discovery

After comparing with current OpenAI plugin developer docs (https://developers.openai.com/plugins/build/auth and https://developers.openai.com/plugins/build/mcp-server), a private user-authenticated MCP plugin is expected to support **OAuth 2.1 authorization code + PKCE S256**, protected-resource discovery, issuer discovery and token validation; supported ChatGPT client identification includes CIMD/DCR/preconfigured clients. The existing DEV-47 provisional static hashed bearer JSON alone is **not a demonstrated compatible ChatGPT plugin sign-in flow**. Do not misrepresent DEV-47/50 as a fully connected ChatGPT plugin.

## Actual implemented code and exact test evidence

Added `development/0.9.27/engram/KiComEngramOAuthTransactions.php` and `test-engram-oauth-transactions.php`; dedicated workflow `.github/workflows/test-0927-engram-oauth-transactions.yml` on branch. Code provides a private SQLite OAuth authorization transaction core, requiring operator-pinned ChatGPT CIMD client ID + exact redirect URI + canonical KiCom MCP resource, PKCE S256, admin-session-bound pending request, separately explicit consent and an already freshly verified passkey fingerprint resolving to the actual original private owner-registry schema. It only issues a code ONCE after consent; token exchange binds code+PKCE+client+redirect+resource and consumes the code transactionally. Tokens are opaque, stored as SHA-256 hashes, short lived (1 hour), revocable; verified token resolves the INTERNAL fixed DEV-44 connector ID + original owner credential fingerprint/binding + host evidence ID. Read-only scope `engram.read`. No refresh-token facility yet. The signature verification itself MUST be done by the original KiComPasskeyBridge inside an authenticated admin route; this class takes the result and is NOT a passkey verifier, public HTTP route, issuer metadata server or self-activation system.

First CI run **35585343386** failed due to an incorrectly delimited redirect URI regex (the `#` delimiter appeared inside a regex character class), with PHP warning. It was corrected, and the test rerun succeeded. Green exact tested commit: **`81942d05465ac66b19969e372cc1894ecdbd4b8b`**. GitHub Actions run **35585455949**, job **106287632658**, conclusion **success**, R3 original ZIP/source manifest verified, two PHP syntax checks green, **28/28 synthetic positive/negative OAuth transaction tests passed** including missing consent, foreign original admin session, nonbound/revoked passkey, wrong PKCE, wrong redirect/client/resource/scope, one-use code, token expiration/revocation and exact owner/host/connector identity. Run: https://github.com/cschymura/kicom-update-mirror/actions/runs/35585455949 .

## Strict status, remaining required work

This is a GitHub development-source component + CI; **NOT** a complete standards-conformant OAuth authorization server, not in installed KiCom 0.9.31, not a ready 0.9.32 native update and not productive on All-inkl. Real OAuth endpoints (protected-resource and issuer discovery, authorization, token and first-party consent page with real signed WebAuthn), supported ChatGPT callback pinning and client identity validation, integration of this token store with DEV-44/48 runtime, HTTP 401 challenge and actual Work plugin registration must still be implemented and tested before independent new-chat recall. Do not deploy private memories or publish tokens/keys to GitHub/Slack. No productive activation without real host review and explicit operator approval. The user should not be asked to repeat any previous SQLite or owner-enrollment steps. Prioritize completing the full OAuth+MCP connection rather than shipping another inert intermediate ZIP.
