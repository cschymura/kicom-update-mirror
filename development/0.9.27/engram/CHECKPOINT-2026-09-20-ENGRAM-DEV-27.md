# KiCom Engram — DEV-27: cryptographically signed original KiCom WebAuthn review + first-party JSON transport

Date: 2026-09-20. Christoph's priority remains a usable PRIVATE Engram across actual, independent ChatGPT instances. iOS triggering works and should NOT be recreated or changed; Slack handoffs work. Never ask Christoph to manually shuttle code, re-run synthetic Mac/iPhone backup routines or execute recurring terminal commands. Public GitHub and Slack contain ONLY code, synthetic data/test reports and handoff metadata, never actual Engram memories, session tokens, passkeys, private host paths, encrypted backups, or secrets.

## Real new code and executed test

NEW `KiComEngramWebAuthnApprovalController.php`: a self-contained, isolated (unregistered, NOT live) server-side approval controller that reuses the ORIGINAL KiCom R3 `KiComPasskeyBridge::createAuthChallenge/assertionOptions/verifyAssertion`. It binds one WebAuthn challenge to one already rendered, exact private draft + matching CSRF + original first-party browser session hash + the owner and the seven-field exact-memory binding in a private 0700 SQLite challenge ledger. WebAuthn verification actually checks origin, RP ID, user-verification flag, enrolled P-256 public key and signature; the returned credential ID is mapped through the private operator-provisioned passkey-owner registry and must match the same authenticated browser owner and namespace/permission. A failed/foreign assertion consumes the challenge and requires a new challenge; replay cannot re-authorize a second memory. A verified request-scoped internal attestation alone enables the prior `KiComEngramFirstPartyReview::confirm` and `KiComEngramPrivateConsentLedger::issue` for THAT displayed record; no caller-supplied 'fresh_passkey_assertion' or 'approved' flag can grant authorization through this controller. NEW `KiComEngramFirstPartyReview::inspect` reads only the same-owner currently displayed pending draft binding after its actual CSRF check, never exposes the raw record via the JSON transport.

NEW `KiComEngramWebAuthnReviewHttpAdapter.php`: unregistered first-party JSON-only transport for `ENGRAM_REVIEW_BEGIN` and `ENGRAM_REVIEW_CONFIRM` using server-injected real first-party browser session lookup, POST/HTTPS/exact Origin enforcement, bounded JSON, strict operation fields, no-store/sanitized responses. Request-supplied subject and cross-origin traffic have no authority. It is NOT a production route and does NOT itself implement or verify the actual KiCom first-party browser cookie/session: host MUST supply a genuine first-party session resolver. A DEV bearer cannot substitute. A genuine browser UI has not yet been wired to these JSON operations (DEV-26 HTML form still needs WebAuthn JavaScript/browser-controller integration). Do not claim an end-user-ready live approval page.

Updated NEW `test-engram-webauthn-approval.php` executes the REAL ORIGINAL KiCom WebAuthn verifier with SYNTHETIC, locally generated P-256 private/public-key test credentials and actually signed WebAuthn assertions. Tests valid/invalid signatures, foreign signed passkey/owner, wrong first-party browser and CSRF, replay, exact one-record consent, private SQLite write, fresh independent KiCom DEV session read, HTTPS/Origin/session HTTP transport rejects and a second signed HTTP-accepted memory write and independent recall. This is significantly stronger than earlier Boolean 'fresh auth' fixtures, but credentials are still synthetically generated and NOT a live human passkey on the user's server.

Initial HTTP adapter CI run 35526380702 failed on a malformed trusted-origin regular-expression delimiter; source corrected and re-run. Final tested CODE SHA `67d5757670fe9f0022ee8d7cb34d061b3c6f7932`, GitHub Actions `KiCom private Engram DEV` run **35526515031**, job **106119514201**, result **success**. `KICOM_ENGRAM_REAL_WEBAUTHN_APPROVAL_TESTS_PASSED=24`, and all original Engram suites passed. The fixed-list NON-INSTALLABLE review-only source archive now includes `source/KiComEngramWebAuthnApprovalController.php`, `source/KiComEngramWebAuthnReviewHttpAdapter.php` and `tests/test-engram-webauthn-approval.php`. Native KiCom verifier prints `ENGRAM_STAGING_REVIEW_VERIFIER_OK=1`, and `ENGRAM_STAGING_REVIEW_INSTALL_ATTEMPTED=0`. Link: https://github.com/cschymura/kicom-update-mirror/actions/runs/35526515031

## Next tangible integration boundary

1. Wire a FIRST-PARTY browser UI (JavaScript navigator.credentials.get with original KiCom challenge) to these two HTTP operations, register a new disabled-by-default actual KiCom route under genuine first-party browser-session controls, with fixed trusted host config and HTTPS/Origin/no-store/CSP protections. Existing KiCom DEV bearer does not by itself prove a first-party browser identity.
2. Verify genuine host private filesystem & same-PHP-UID/vhost/alias isolation; server private content is NOT encrypted at rest by this code. Implement real owner registry provisioning, backup/rollback/retention and deliberate real-data consent. Existing remote Bootstrap or GitHub mirror is not a personal Engram store.
3. Build ONE complete, native-verifier-reviewed and rollback-capable real KiCom update package. The previous 0.9.27 REVIEW-ONLY host-probe candidate does NOT install Engram. Obtain Christoph's explicit approval only when the precise protected production deployment is ready. No paid action authorized.
4. After such authorized installation, verify a synthetic write on the REAL server and read by an entirely NEW, real independent ChatGPT conversation through an AUTHORIZED connected KiCom/Engram connector. Nothing in DEV-27 establishes the latter yet. Do not report Engram LIVE or real memory saved until verified on-host.

If another Handwerker cannot access Slack/BOOTSTRAP, continue from this exact GitHub checkpoint and concrete code via the available GitHub connector; do not merely run nine failed URL retries or issue a generic OFFLINE_ERGEBNIS.
