# Mirage Engram DEV-36 — KiCom 0.9.29 native update ready for user-approved live synthetic SQLite test and signed owner binding

Date: 2026-09-20. Parent: DEV-35 checkpoint commit `11a88baff515d0405cc4bf8ccbfd55e53b355167`, branch `work/kicom-0.9.27-pam`. Christoph shared actual KiCom admin screenshot showing `SCAFFOLD_PREPARED_DISABLED` and `PREFLIGHT_ONLY`; private directories, config and owner registry exist, `sqlite=ready`, `data_database=missing`. The screenshot confirms the scaffold status, **not** actual SQLite persistence or a connected ChatGPT memory.

## New, actually developed source and native release

- Native complete release `KiCom-0.9.29-Mirage-Engram-LIVE-TEST-INACTIVE.zip`, 226557 bytes, 54 files; SHA-256 **`f8781b0ba098749c32c30e80aa1bcfe8209b00e7bde5290d2be7ab4d78a34720`**. Exact parent archive `KiCom-0.9.28-Mirage-Engram-Setup-DISABLED.zip` SHA-256 `53d251c2d522fbdb6f4725879ffe5f8223e5b255be35ea3a2480f470ab48e6ac`.
- New first-party `modules/engram/KiComEngramNextStep.php`, first-party static `assets/mirage-owner-client.js`, reviewed additions to original authenticated `admin.php`, version constant, native modules/genome SHA manifests. Critical production `api.php`, `guardian.php`, `recovery.php`, `index.php`, `living.php` and htaccess files are **byte-identical** to KiCom 0.9.28.
- Authenticated admin page `admin.php?engram_setup=1` now supports a **separately confirmed, synthetic-only real-server SQLite roundtrip**. Only when original password-authenticated KiCom admin session + existing CSRF + HTTPS + exact confirmation `MIRAGE TESTDATEN` are present, opens the already prepared private data dir, creates a *missing* SQLite file with 0600 permissions via existing KiComEngramStore, inserts randomly generated synthetic content under `synthetic-admin-preflight/diagnostic`, closes connection, reopens via new PDO connection and checks exact read and cross-owner isolation. It never reads a private user memory or activates the public/private API. If database already existed, refuses to overwrite it.
- New `admin.php?engram_owner=1` binds an **already enrolled original KiCom passkey** to the private registry's sole fixed owner `mirage-owner`. Uses original KiComPasskeyBridge's fresh signed P-256 assertion, user verification, same original admin session + CSRF + HTTPS + same-origin JSON transport, 120-second single-use challenge; registry holds only fingerprint/subject/scoped read-write permissions, never raw secrets. Existing mapping is never overwritten; no anonymous/public enrollment.
- The host config remains `enabled=false`, `operator_approved=false`, `host_isolation_verified=false`, `review_enabled=false`, `runtime_source=setup-pending`, `private_memory_scope=setup-pending`. **Even after successful live synthetic test and owner binding, real personal Engram remains OFF.**

## Actual local verification and limits

- Existing original KiCom 0.9.28 self-update verifier accepted the exact new 0.9.29 release against a disposable original 0.9.28 installation. Risk class **RED**, extra human admin approval required; unchanged recovery kernel, correct genome parent/generation. All 36 packaged PHP files lint clean, JS `node --check` clean, manifest and component digests verified; changed paths exact.
- **19/19 local synthetic admin/passkey tests PASS**, including real P-256 signature using original KiCom bridge, invalid signature, foreign session/CSRF/HTTPS, replay and never auto-enabling. The class was extracted from the actual final ZIP and all 19 tests re-run against its exact packaged bytes. **18 independent release verification checks PASS**, including three intentionally corrupted ZIPs rejected by original native verifier even after manifest recomputation.
- IMPORTANT: local PHP has openssl/sodium/PDO but **no pdo_sqlite**; therefore the real SQLite roundtrip is **NOT yet tested**. User's actual All-inkl admin reported `sqlite=ready`, but the real write/read must be executed only after operator-approved installation. The signed owner unit test uses an explicitly synthetic dummy DB presence fixture and **does not prove a real DB roundtrip**.
- The release ZIP and separate source/test bundle `Mirage-Engram-DEV36-Quellen-und-Tests.zip` (SHA-256 `f7ca2bdaaa7f2518115e1345f39b7b01dfdc77c5b1401db6eb88b86450d38834`) exist **only as downloadable attachments in current conversation**, not as a GitHub binary release or deployed server files. A native CI Actions run was **not** invoked; only the actual local original KiCom native verifier was run. No update feed or production server was modified by this developer.
- Explicit instruction: install only the **native complete 0.9.29 release ZIP** via existing KiCom admin update / check / separately confirm target `0.9.29`; never upload the source/test bundle or deploy PHP files by FTP. Then run synthetic test via admin and existing passkey binding; share only non-sensitive result codes.

## Exact next milestone

After screenshot or authenticated confirmation of `SYNTHETIC_SERVER_SQLITE_ROUNDTRIP_OK` and `PASSKEY_OWNER_BOUND_INACTIVE`, separately verify actual ALL reviewed vhosts/aliases/host PHP UID/open_basedir and cross-app same-UID access. The 0700/0600 directories alone do not establish isolation against sibling apps under the same PHP UID (many sibling webroots were visible in user's FTP screenshot). Validate private backup/restore and retention; only then prepare and separately authorize trusted host-runtime activation. A separate actually connected ChatGPT MCP identity/connector and new independent chat retrieval remain pending. Do not claim private memory live merely from synthetic DB/owner setup.
