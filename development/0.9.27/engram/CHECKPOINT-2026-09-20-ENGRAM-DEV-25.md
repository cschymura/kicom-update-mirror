# KiCom Engram — DEV-25: integrated private-memory synthetic acceptance complete

Date 2026-09-20. Christoph's priority is a functional, personally usable Engram in new ChatGPT instances, not repeated diagnostic loops or new iOS scripts. His existing iOS time automation and Slack communication already work in demonstrated trials; no operator Terminal scripts, iPhone shortcut recreation, manual handoff or repeated synthetic backup tests requested. No sensitive user content, real keys, tokens, private memory or actual host paths are contained in public GitHub/Slack.

## Tangible new implementation and successful executed tests

Completed DEV-24: new isolated `KiComEngramPrivateConsentLedger.php` supports independently reviewed, EXACT-record, time-bounded one-time approval receipts using private SQLite IMMEDIATE transactions (one pending receipt per seven-field binding); never stores raw memory. `test-engram-private-consent-ledger.php` tests that writes without consent, replay, mismatched subject/body/provenance/namespace and insecure storage are rejected, while a genuinely separately issued synthetic session can retrieve an approved record. Corrected first failed synthetic CI test fixture: PHP arrow closure had captured the synthetic identity table before it was extended to the second session; repaired by capturing mutable synthetic fixture by reference. Initial CI 35524626610 FAILED; subsequent CI succeeded.

Added NEW `test-engram-integrated-private-memory.php` full-stack executable acceptance using the ORIGINAL KiCom 0.9.26 R3 DEV session manager (synthetic verified-passkey session records), protected session credential reader, separately provisioned 0600 non-webroot private owner registry, independent-review *synthetic callback*, private 0600 one-use consent ledger, HTTP-like memory adapter, private Engram SQLite store, and a FRESH independent authenticated session with a NEW adapter/registry/ledger/SQLite connection. It stores exactly one synthetic memory and retrieves it from the new session without sharing a prior chat's context or copying the raw SQLite record; a different registered owner cannot see it. The test explicitly denies preapproval writes, replayed consent, unauthorized cross-user read/write and revoked session access. This is an integrated development test of KiCom components, NOT two actual ChatGPT sessions using a live connector.

NEW acceptance `KICOM_ENGRAM_INTEGRATED_PRIVATE_MEMORY_TESTS_PASSED=10` and existing new consent `KICOM_ENGRAM_PRIVATE_CONSENT_LEDGER_TESTS_PASSED=25`. Fixed-list source-review-only ZIP includes both consent implementation and full-stack acceptance test. Last code SHA **`82e3889acea8553331eb1ffc872ff4ebd5e626cd`**. Github Actions workflow `KiCom private Engram DEV` run **35524921501**, job **106115299567**, result **success**. Logs also show `ENGRAM_STAGING_REVIEW_VERIFIER_OK=1`, `ENGRAM_STAGING_REVIEW_INSTALL_ATTEMPTED=0`. Link: https://github.com/cschymura/kicom-update-mirror/actions/runs/35524921501

## EXACT remaining live tasks — no handwaving

1. Current code is ONLY a synthetic, disconnected DEV prototype. There is still NO deployed Engram HTTP endpoint on real KiCom, NO real private Engram read/write, NO identity-scoped ChatGPT connector and NO evidence that independent real ChatGPT sessions can fetch data from actual KiCom.
2. The private ledger's `independentReview` callback is a synthetic stub in tests: build an ACTUAL first-party human-authenticated "review exact displayed record / approve one record" UI + private server-side issuer, independently scoped to subject and exact binding. An ordinary authenticated DEV session or an LLM/Slack message must NOT mint approval by itself; no client-supplied `approved` flag.
3. Integrate the already implemented KiCom DEV session file reader, operator-provisioned private owner mapping, consent ledger, memory adapter and private storage behind an AUTHORIZED, disabled-by-default first-party route. Keep runtime private config and real data outside public webroot, source control and Slack. Verify host vhosts/alias/default-host, same-PHP-UID cross-app isolation, approved owner provisioning, bounded auth/scopes/revocation and backup/restore/retention before live real private memories.
4. Prepare ONE complete, native-verifier-reviewed, rollback-capable protected KiCom update package. The existing KiCom 0.9.27 REVIEW-ONLY host-probe candidate DOES NOT contain a deployable live memory feature and must not be represented as one. Production installation requires Christoph's explicit, precise approval only when ready to apply the actual vetted package. No paid action authorized.
5. After permitted installation, conduct a sanitized synthetic memory write then independent REAL ChatGPT instance read via the actual authorized KiCom connector. Only then mark Engram as live; migrate real private memories only with an explicit scoped consent. The current code's private ledger is NOT encryption at rest; the user's previous synthetic encrypted Mac/iPhone/external backups are separate and do not establish live server encryption.

Avoid repeating failed BOOTSTRAP follow-up probes. A new Handwerker may continue directly by fetching this checkpoint and the exact listed files with available GitHub access; commit actual tested code and test evidence rather than merely returning OFFLINE_ERGEBNIS. Do not confuse public technical project memory with the future private Engram database.
