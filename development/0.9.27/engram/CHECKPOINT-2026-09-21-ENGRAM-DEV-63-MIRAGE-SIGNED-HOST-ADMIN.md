# Mirage Engram DEV-63 — original signed KiCom admin hosting-policy preparation, CI GREEN

Date: 2026-09-21. User requested continued development **without Work**, with actual live admin/plugin commissioning postponed until **2026-09-22 00:00 Europe/Berlin**. Exactly one previously created reminder already exists; no new automation is required. Existing All-inkl shared hosting is accepted by Christoph. This is a real operator decision, NOT proof of PHP UID isolation; no migration or repeated debate.

## Starting state, conflict check

Checked branch `work/kicom-0.9.27-pam` and shared Slack; DEV-62 was already completed before this work began, so its binary release and plugin draft were NOT rebuilt. The DEV-62 full native **0.9.33** direct 0.9.32-parent candidate, SHA256 `0287bdaf9ea92f3e4cd6de5080e22e848780b1b0c5d340d23b17acf36cdd5372`, is still PREPARED but NOT installed (latest operator-confirmed live KiCom **0.9.32**). The older DEV-60 0.9.33 ZIP is superseded; NEVER install both 0.9.33 versions. DEV-62 plugin/mcp JSON are only an uninstalled portable draft. Exact DEV-62 checkpoint: `CHECKPOINT-2026-09-21-ENGRAM-DEV-62-MIRAGE-0933-SHARED-PLUGIN.md`.

## Actual code implemented

1. `KiComEngramSharedHostAdminConfig.php` is an original authenticated-KiCom-admin, CSRF-, HTTPS- and genuine original-WebAuthn-P256-signature-bound first-party transition of *existing real private* `engram-host.json` from `setup-pending` to `server-only-reviewed` with exact **operator-accepted shared-host** policy, no invented isolation claim. The operator must explicitly confirm the known limitation; the original registered passkey fingerprint must resolve to the currently active protected Engram owner registry with `engram.read`. The code pins stable ChatGPT client ID+callback, connector ID, owner binding, and host-snapshot ID derived from the original private config SHA, exact webroot and verified owner. It **keeps ALL public/private activation flags disabled** (`enabled`, `operator_approved`, `review_enabled`, `oauth_enabled`, `mcp_connector_enabled` false; `host_isolation_verified=false`). A signed hosting decision alone cannot create OAuth tokens, activate the DB, or expose the MCP endpoint. It preserves existing owner/passkey/private-memory files. It checks existing protected file/dir modes, actual original root/schema, session-bound CSRF, signed challenge, expiry and config hash again before a single private atomic swap. Saves a unique immutable private 0600 backup of the original host config first.
2. `KiComEngramSharedHostAdminHttp.php` and `mirage-host-policy-client.js`: genuine first-party **authenticated original admin** GET review + CSRF-protected JSON challenge and confirm, honest shared-host limitation displayed, explicit checkbox plus fresh KiCom Passkey/WebAuthn assertion. Foreign origins, wrong/missing CSRF, anonymous access, HTTP, injected owner/host request fields, wrong signature and replay are rejected. No credentials or private memories are transferred to JS or public GitHub.
3. `DEV63_0933_ORIGINAL_ADMIN_SHARED_HOST_POLICY.patch`: source-only integration patch for original first-party `admin.php?engram_host_policy=1`, loading the existing KiCom PasskeyBridge from the trusted server root and serving the signed admin UI. Source patch itself is in GitHub; it is **NOT YET MERGED INTO A FULL NATIVE BINARY** and the patch's full application to the exact DEV-62 binary has NOT been verified in this work segment. Do not imply native 0.9.33 has the DEV-63 admin route.

## Verified results and source handoff

The first CI run `35619468177` failed because the anonymous test passed a literal array to a by-reference PHP parameter; corrected. Subsequent HTTPS-signature change initially left one test call missing the new boolean, corrected. **FINAL** original KiCom signed admin hosting-policy source/test SHA `4f5146d58df889e94c4d856e75b9e87e1bbc35ed`. GitHub Actions run **35620611074**, job and workflow **SUCCESS**: **31/31 synthetic genuine P-256-signed KiCom admin/CSRF/origin/rollback/owner/stable-client/old-data-preservation/backup assertions**, six PHP lint checks and JS syntax green. The authenticated HTTP admin review/challenge/confirm path was exercised within the synthetic fixture; no remote All-inkl browser POST or real Christoph Passkey was used. Test run: https://github.com/cschymura/kicom-update-mirror/actions/runs/35620611074

Exact source+UI+patch CI handoff commit `ffdfbd5883091a5ea7148c367c6a04a521cd77b3`, GitHub Actions run **35620718350 SUCCESS**, artifact `mirage-dev63-exact-first-party-host-admin`, artifact ID `10648465747`, retains exact PHP/JS/patch bytes and their SHA256 manifest for the later native build. https://github.com/cschymura/kicom-update-mirror/actions/runs/35620718350

## Remaining work / no false success

**NO** live admin configuration change, actual Passkey approval, OAuth/activation DB mutation, private-memory activation, plugin registration or actual independent ChatGPT recall has happened. Current live KiCom 0.9.32 remains unchanged. The DEV-63 source is NOT in the prepared DEV-62 0.9.33 ZIP. Next: merge/test exact DEV-63 admin+UI/JS+patch into the genuine full native 0.9.32→0.9.33 successor (or stage another monotonic version only if 0.9.33 is already installed; do NOT attempt same-version self-update). Then add a separately signed per-owner activation route without conflating hosting acceptance and memory approval; complete tested normal-ChatGPT manual plugin connection and real synthetic new-chat recall. Honor user's requested deferment until 00:00 for live installation/connection; do not promise background work, create a duplicate reminder, reopen the hosting-migration debate, request old SQLite/Passkey tests, or call this genuine live cross-chat memory yet.
