# MIRAGE DEV-95 — Real original-admin route for ACTIVE private DB additive upgrade

Date: 2026-09-23. **DEVELOPMENT ONLY. No full package, no live installation or migration, no OAuth write activation.**

## Exact-source baseline
- Last reported live KiCom 0.9.37, independent cross-chat synthetic `engram_search` READ previously succeeded. This turn confirmed the exact original conversation ZIP is available locally: `KiCom-0.9.37-Mirage-MCP-META-FIX-VOLLSTAENDIG.zip`, SHA256 `0c6e02c64d44d603cb229f562d189f1b798bd78c2188fd2907e6b5cbafdb5ea7`.
- Prior DEV-94 ONE original-native OAuth transaction merge stages the previously conflicting DEV82 read refresh and DEV93 read+write consent changes. **Do not apply old DEV82 exchange patch again on top of DEV94.** DEV94 still has no validated full native package or on-host ChatGPT reconnect proof.
- Work branch `work/kicom-engram-dev95-active-db-upgrade` descends from DEV94 checkpoint SHA `b9c2bcb3ba9bb35167a2ebaede74a6f070c1bc3a`.

## Actual committed new modules and original admin entrypoint
1. `dev95/KiComEngramActiveSchemaUpgrade.php`: strictly server-derived original web/private paths and ACTIVE runtime/host validation. READ-only `preflight`. `applyAfterVerifiedPasskey` uses original operator-provisioned private OAuth+Engram SQLite files and PRIVATE backup directory, takes a nonblocking private lock, runs SQLite `VACUUM INTO` (WAL-consistent) to TWO unique 0600 private backup snapshots, reopens and verifies each backup's `PRAGMA quick_check` BEFORE DDL. Calls the already merged original-native `KiComEngramOAuthTransactions::prepareWriteSchema` and `::prepareRefreshSchema` plus DEV87 `KiComEngramMutationSchema::prepareNew/assertReady`. Creates a 0600 random private `mirage-write-signing.key` only if absent; repeated preparation never changes an existing key. Does NOT set `engram_write_ready`, issue an OAuth token, approve a chat, alter owner rights, or change original read tokens and memories. If a schema preexists malformed it fails closed; partial two-DB migration is NOT cross-database atomic, so private backups remain and all write runtime flags must stay OFF until verified.
2. `dev95/KiComEngramActiveUpgradeAdminHttp.php`: actual first-party original admin/session + CSRF + exact HTTPS/Host/Origin guard, one-use 120-second ORIGINAL KiComPasskeyBridge fresh WebAuthn owner assertion and original private owner registry lookup. An operator-approved live READ owner is required; no fake `engram.write` rights are auto-assigned by preparation. Original runtime identity hash must remain unchanged between challenge and signed confirmation. It invokes the migration module ONLY after successful fresh signed owner assertion, never on GET or mere password authentication. No POST-controlled owner, path, scope, consent, DB or config.
3. `dev95/mirage-active-upgrade-client.js`: single straightforward admin control labeled **FREIGABE**. It uses the existing origin-bound WebAuthn challenge/assertion wire format; it shows success only after both backups and schema preparation. No private DB/file paths in browser response.
4. `dev95/NATIVE-original-admin-active-db-passkey-upgrade.patch`: insertion into the actual original `admin.php` next to the existing authorized first-party owner activation route. Loads only the original server runtime/session/CSRF/fixed web root and original passkey bridge, then passes through a strict response/header allowlist. The original admin file and complete installed native source tree are not yet committed or packaged as a full ZIP. The DEV95 PHP source needs to be flattened into `modules/engram/` and JS copied to `assets/` during the **ONE** final package; do not install this patch standalone.

## Independently verified CI
Run **35864729300**, job **107193316394**, SUCCESS, genuine PHP 8.2 + ext-pdo_sqlite:
- 4 PHP syntax files clean.
- **26/26 genuine PDO SQLite SYNTHETIC active-DB** checks: original read-token and memory content retained, absence of DDL in preflight, both pre-DDL WAL-consistent private backups created/integrity tested, additive OAuth refresh/write-consent + canonical mutation schema, correct private signing-key preservation, idempotent second run, denied path substitution/runtime activation.
- **17/17 synthetic original first-party ADMIN HTTP guards** checks (admin/Origin/CSRF/session/challenge lifecycle, denied failed/replayed/expired assertion, one-button FREIGABE). This suite intentionally uses an explicitly FAKE Passkey verifier and FAKE schema mutator to isolate HTTP control-flow; it DOES NOT prove original real WebAuthn signature verification or production migration.
- **43/43 synthetic assertions, 0 failed**. https://github.com/cschymura/kicom-update-mirror/actions/runs/35864729300

## Additional follow-up guard

A further malformed-table condition was fixed after the initial checkpoint: mere presence of expected columns in the new refresh and consent tables does not prove the required UNIQUE constraints. The operator-only migration now verifies the actual private SQLite index metadata for UNIQUE(parent_access_hash) and UNIQUE(token_hash), and rejects an incompatible preexisting table instead of silently accepting a weaker state. GitHub Actions run **35865076104**, job **107194504865**, completed SUCCESS after this source change; the existing 43 synthetic PDO/HTTP assertions passed. The dedicated malformed-same-columns-but-missing-UNIQUE negative fixture should be added before release. This run also does NOT demonstrate real WebAuthn or actual native exact-parent package integration.

## Precise remaining blockers before READY_FOR_RELEASE
1. Reapply and reconcile exact original-native DEV90/91/92/93/94 patches into ONE complete 0.9.37-descended source tree; stage DEV95 files in flat native paths; verify every unified diff against exact parent and full-tree PHP lint. DEV93 and DEV94 change the same OAuth source, so do NOT apply incompatible older DEV82/93 standalone exchange patches in parallel.
2. Test DEV95 against the ACTUAL original merged `KiComEngramOAuthTransactions.php`, actual `KiComEngramStore` SQLite layout and actual KiCom PasskeyBridge/admin route with real PDO SQLite/HTTP in a **nonproduction** environment; CI DEV95's OAuth DDL class matches expected shape but is a TEST DOUBLE, not the full original code. Audit backup permissions, absence of accidental write activation, partial migration and recovery.
3. Add operator-controlled current owner WRITE right assignment/revocation and independently approved native Passkey/OAuth combined `engram.read engram.write` ready configuration (NOT automatic upgrade of old READ bearer), plus genuine original `api.php` DEV92 write factory and DEV91 current private owner/consent gate; complete both old-read + write/refresh native MCP/HTTP positive/negative regression and new-chat double-consent isolation.
4. Validate exact original manifest/genome/installer/backup/recovery/rollback and full-package SHA; ONLY ONE fully tested installable whole KiCom candidate for Christoph's separate production approval. After one approved real new-chat/iPhone OAuth trial, if the failure persists STOP automated auth changes and jointly inspect evidence with Christoph. ChatGPT-platform-owned prompts are not controlled by KiCom.
5. Optional FTS5/semantic search not enabled or claimed in DEV95; no private memories, bearer tokens, credentials, backups or user-specific contents transferred to GitHub. No manual action by Christoph is required for current development.

This checkpoint is source history, NOT a downloadable or installable complete KiCom release.