# KiCom Engram — DEV-20 reviewed, verifier-accepted 0.9.27 host-probe candidate; NO installation

Date: 2026-09-20, local operator target was 15:30 CEST for live Engram. The user expressly requested minimal manual intervention, safe autonomy and an integrated release rather than repeated terminal-command interpretation. These instructions are also maintained in COLLABORATION-PROTOCOL.md. This checkpoint is technical handoff: public repository contains ONLY program code, synthetic fixture tests and verification metadata. Never commit actual memories, original chats, user identifiers, private database/backup bytes, private signer/decryption keys, real KAS paths/configuration, tokens/OTP, private retrieval results or receipts.

## Latest verified live and local status

- Live KiCom BOOTSTRAP, PROJECT_STATE, ARCHITECTURE, PROTOCOL, DECISIONS, CHANGELOG and NEXT re-read during this cycle. Live KiCom remains **0.9.26**; Opera read-only Control Plane displayed installed genome `kicom-0.9.26-g25r3` and healthy status. These observations are not proof of the exact installed module hashes, or a protected update installation.
- Operator-reported (NOT independently observed through Mac filesystem): macOS 15.8, Python 3.14.7, PyNaCl 1.6.2, FileVault on, local personal Curve25519 private decryption key created mode 0600 and not shared; local synthetic encryption test passed; local synthetic SQLite backup passed; encrypted synthetic 8240-byte SQLite test bundle copied to existing external HFS+ drive and independently reopened/restored with two synthetic entries; separately password-encrypted private-key recovery JSON copied to local iPhone Files folder and locally decrypted/compared in a prior test. These are only local synthetic checks; no LIVE server personal-memory export or memory engine was installed. Never ask for the private key or the recovery passphrase. Do not repeat the individual Mac setup steps. Future operator action must be a single consolidated, idempotent application/installer step.
- Opera can READ authenticated/all-inkl page content but exposes no supported click/upload/update action. TinyFish's browser automation is METERED and not authorized by the user's no-cost instruction; do not spend wallet credits or perform a protected action merely because a session is open. No KAS/WebFTP mutation was attempted.

## New executable work — a real KiCom update-structure CANDIDATE, not a memory-service release

- New `development/0.9.27/engram/build_staging_host_probe.py` is a deterministic, offline, pinned-source release CANDIDATE builder. It revalidates exact original 0.9.26-R3 archive SHA-256 `6e93e7b176ce429a922cb5e5906e90046f68c38fb3a8fb1b5cabcd529b56dd1f` and every original source MANIFEST entry. Uses the existing hash-pinned DEV candidate patcher; adds only `DEV_ENGRAM_PATH_PROBE` capability/handler to the original KiCom DEV session/router/endpoint, copies unchanged DevHttpAdapter, adds two reviewed private-path probe/handler modules to the trusted module list/genome, advances version metadata to 0.9.27 and genome `kicom-0.9.27-g26-engram-probe` (parent `kicom-0.9.26-g25r3`, generation 26), updates every modified component SHA and full ZIP manifest. All originally protected kernel/index/api/guardian/perimeter content remains byte-identical.
- **Crucial**: The real trusted private filesystem configuration function `kicomEngramDevTrustedConfig()` is NOT present in this ZIP. Without operator-reviewed server-side registration and the actual complete webroot/alias inventory, the new handler remains INERT. This candidate does NOT provide full Engram live personal memory ingest/retrieve, does not export a real backup and must not be misrepresented as the user's requested live system. It also does NOT install anything by itself, stage the live update, or publish to an automatic update feed.
- The dedicated Engram CI workflow builds this candidate only AFTER running the full 351 existing synthetic checks and an original R3 package/source preflight, then verifies the resulting ZIP with the original, unmodified KiCom `kicomSelfUpdateZipInspect()` and `kicomSelfUpdateRiskClass()` in a disposable R3 runtime. It REQUIRES a valid child version/genome, unchanged kernel/index/api/guardian/.htaccess, no runtime private state in ZIP, real memory ingestion disabled, risk classification RED and an independently gated human approval; artifact is uploaded to GitHub Actions for inspection and not to KiCom/All-inkl or a release feed.
- Initial run **35511050314** FAILED `GENOME_COMPONENT_HASH_MISMATCH` because a version-only `lib.php` change was missing its updated component SHA. The builder was corrected. Do not cite the initial failed run as a pass.

## Verified executable proof

Tested code SHA: **`9cb555b3de4b0205b08c30e9f23dd94463dbabeb`**.
GitHub Actions `KiCom private Engram DEV` run **35511101449**, job **106079042520**, result **success**. Full 351 synthetic tests passed; original 0.9.26-R3 ZIP/source integrity preflight passed; offline candidate ZIP passed unmodified KiCom native verifier, RED classification, unchanged kernel, missing private config, and non-installation assertions.
- Review-only staging ZIP SHA-256: **`069eff974e075e56ed3f7236ae6e12495fe2ed1f27891f8dbb0027211f85f0e2`**.
- GitHub Actions artifact `KiCom-0927-Engram-Hostprobe-REVIEW-ONLY`, artifact ID **10605181936**; stored ZIP filename `KiCom-0.9.27-Engram-Hostprobe-REVIEW-ONLY.zip`, 14-day GitHub Actions retention, builder persists in repository to regenerate if expired.
- https://github.com/cschymura/kicom-update-mirror/actions/runs/35511101449
- This checkpoint is documentation-only; cite verified executable SHA/run above, NOT its later checkpoint commit, when claiming tests passed.

## Human-controlled blockers and concrete next action

1. Real operator-owned complete KAS/All-inkl vhost/default-host/alias inventory, actual host PHP SAPI effective UID/open_basedir and cross-app same-UID file isolation HAVE NOT BEEN VERIFIED. Existing WebFTP private data/backups preparation, synthetic canary and selected HTTP 404s are insufficient. DO NOT re-request recreation of existing folders or personal keys; only authorize one bounded synthetic host proof through a private, reviewed config channel.
2. The current candidate intentionally lacks a production config provider or verified runtime loader injection into real-host PHP and cannot itself run the synthetic path probe. A separate private-host trusted config injection and preflight design is required before any meaningful host probe. Do not ship a publicly accessible PHP path diagnostic page or place config/private data in public GitHub, Slack or Mail.
3. The new 0.9.27 genome COMPONENT SET change is **RED**, and normal KiCom protected live staging/install must use the existing transaction-bound FreeOTP/approval, verified original genome/package, normal backup/health/rollback and explicit user authorization. User granted package PREPARATION, not an unconditional protected live installation. No install/upload has been performed.
4. True private long-term memory also still lacks independently proven same-UID isolation, user identity-bound ingestion/retrieval connected to actual KiCom runtime, real consent receipt issuer, validated across-all-replicas retention/hard erasure and independently protected real signer/public trust root, hence cannot safely assert “live by 15:30”. For zero additional costs, the user-owned Mac/external encrypted disk can be the offline encrypted backup/decryption key home but is not currently an online private-memory service, and it cannot independently protect server plaintext accessible to a compromised same-UID PHP app.
5. STOP autonomous changes at the actual host-trust/approval boundary. The next response to user should describe tangible package/checkpoint and the precise remaining blocker, with AT MOST one consolidated user decision/action. No more manual terminal command lists or invented automatic cloud/browser actions, no false deadline assurance.
